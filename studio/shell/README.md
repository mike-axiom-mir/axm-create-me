# AXM Create-Me 3D Studio shell

State: **BOUNDED READ-ONLY SHELL / ACCEPT FOR TEST**

Open [`index.html`](index.html) directly. The shell has no package install, account, network,
cloud, API, or build requirement. GitHub links are optional exits; all dashboard state and preview
assets are local.

Run `node verify-shell.mjs` from this directory to check the bounded data contract, local assets,
exact source identities, and every linked Studio status record.

Pull requests also run `browser-proof.spec.cjs` in Chromium at 1440 × 1000 and 390 × 844. The
workflow exercises filtering, search, exact-identity inspection, and dialog closure, then retains
fresh overview/detail screenshots and an observation receipt for each viewport.

## Job

The shell makes the current 3D specialist constellation understandable without changing its state.
It provides:

- one exact-source card per domain repository;
- accepted / hold / support / no-output filters;
- search and keyboard-accessible specialist inspection;
- a first environment vertical-slice assembly view;
- direct access to the 14 cross-domain Studio status records;
- an explicit rule for adding future category owners.

It does **not** merge branches, execute product generators, promote candidates, rewrite status,
replace GitHub, or centralize domain knowledge into Create-Me or Universal Creation.

## Visual contract

- **Identity:** a future-luminous workshop control room where source truth is easier to see than hype.
- **Signature:** the environment dependency rail visibly separates accepted sources from held consumers.
- **Palette:** graphite/ink surfaces; cyan coordination; green accepted main; amber holds; grey absence.
- **Shape:** engineered asymmetric corners and thin provenance lines rather than generic rounded cards.
- **Motion:** hover/focus feedback only; reduced-motion preferences are respected.
- **Detail budget:** strongest detail sits in actual retained previews and exact identity/gate information.
- **Anti-defaults:** no fake live telemetry, invented metrics, stock imagery, automatic promotion, or generic admin-dashboard copy.

## Source boundary

Dashboard records were transcribed from [`../CONVERGENCE_STATE.md`](../CONVERGENCE_STATE.md).
The eight retained previews are the visual review artifacts generated from the exact identities named
on their panels. The Character/Animal/Nature/Object/Building/Map panels are neutral inspection
renders; Weather uses its exact repository-native SVG coordinates; Universal Creation is a retained
representative current-main image. These are not new target-host or target-device acceptance claims.

`AXM · Axiom/Mir`
