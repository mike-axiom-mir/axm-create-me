# 3D Art Direction 021 — Building planar-role Godot normal repack

Date: 2026-09-17
State: **PASS_ART_DIRECTION_BUILDING_PLANAR_ROLE_GODOT_NORMAL_REPACK_RETAINED_VIEW_NONDEGRADATION_021 / HOLD_BUILDING_PLANAR_ROLE_NORMAL_TRANSPORT_EXACTNESS__MEASURED_REPACK_REMAINS_REAL / HOLD_DEFAULT_BUILDING_PLANAR_ROLE_ADOPTION__RESIDUAL_PRIMITIVE_TARGET_DEVICE_AND_ENVIRONMENT_DECISION_PENDING**

## Scope

This is an Art Direction coordination decision only. `axm-create-me` remains coordination-only and does not become the Building, Map, Runtime, Technical-Art or Universal Creation product build.

The four AXM roots remain the merge gate:

1. Truth.
2. Agency / non-domination.
3. Continuity.
4. Wisdom before speed.

## Why this is the highest-leverage ready visual gate

Direction 020 preferred the source-owned planar-role Building receiver and already accepted Runtime #39's sparse post-normal indexing raster delta as visually non-degrading in the retained fixed views. Two new independent facts now exist:

- Visual Observer / QA has independently passed the exact indexed current-world receiver across all 68 retained frames.
- Technical Art PR #40 has localized a real post-index normal repack in the exact Godot receiver path: the grouping/position result is structurally correct, but some decoded normals are not mathematically identical after `SurfaceTool.index()` / commit.

That leaves one bounded Art-owned question: does the newly measured normal repack create an observable visual defect in the exact retained current-world evidence?

Other active lanes remain owned elsewhere and are not duplicated here: Runtime PR #41 is testing direct-index construction, Nature timing remains a VFX/capture question, Animal remains held on transported deformed direction-frame equivalence, and Character remains upstream-held by nonzero shoulder intersections.

## Exact reviewed identities

Current Environment indexed receiver:

- repository: `mike-axiom-mir/axm-map-design`;
- Environment PR #24 exact head: `038925282240441c475651bdc3737d1749c31d06`;
- retained indexed artifact: `10482542600`;
- independently downloaded archive SHA-256: `6661e96c1c766ed799ad80b177a4faaeb57c0fefd8e892ac7cdc03beb731c0bb`;
- retained `exact-head.txt`: `038925282240441c475651bdc3737d1749c31d06`.

Exact unindexed planar-role comparator:

- Environment head: `b758f9ca006ec5885ff1c2c52e2fb09e9ccdd464`;
- artifact: `10481340680`;
- independently downloaded archive SHA-256: `96d6bd6a0ed6e6edb0fc129484dd02769f7fd13952ad1a7eabeda8ffe681ee74`;
- retained `exact-head.txt`: `b758f9ca006ec5885ff1c2c52e2fb09e9ccdd464`.

Technical-Art attribution lane:

- Map Technical Art PR #40 exact head: `e4105697802fad082b31da21a22a8dd1a944dedd`;
- retained artifact: `10483020277`;
- artifact SHA-256: `34ce988749547430caf86b36bbba53a1711a9ec4a484005027214c41cea79e55`;
- exact structural result: `PASS_BUILDING_EXACT_GODOT_INDEX_GROUPING_MATCHES_UC_CROSS_SOURCE_TUPLE_CANDIDATE__HOLD_GODOT_NORMAL_REPACK_EXACTNESS_AND_VISUAL_REVIEW`.

Source / material identity remains unchanged:

- Building Hard-Surface PR #11 head: `93f22e4eeb9bb32516d4b11f8d8bcf47d9792910`;
- representation: `boundary-only-planar-role-rectangle-render-001`;
- `336` triangles / `5` material roles;
- Building Materials PR #3 head: `4179aa1401f5a9114399e2f998c96809d4b8ed2e`;
- no material scalar or lighting retune is part of this direction.

## New Technical-Art truth that must remain visible

The exact Godot indexing path does **not** preserve every decoded normal exactly after commit.

Technical Art proves:

- `1008` input triangle corners -> `312` indexed vertices / `1008` indices;
- the real Godot grouping and the explicit UC cross-source tuple candidate form the same corner partition under bijective relabeling;
- decoded position corner streams remain exact;
- exactly `120 / 1008` decoded corner normals change after the Godot index/commit path;
- every changed normal is in `frame_galvanized`;
- maximum absolute component delta: `0.00011304020881702792`;
- maximum angular delta: `0.006869404718583788°`;
- the other four material surfaces retain exact decoded normal corner streams.

This is a real transport/receiver exactness difference. Art Direction does not rename it as equality, round it away, or insert a new generic tolerance into UC.

## Direct retained-view inspection

The indexed and unindexed archives were independently downloaded and rehashed before review. Representative states `0 / 8 / 16` were checked in both `path_eye` and `elevated_oblique`, including enlarged Building crops and amplified differences.

Independent reproduction remains:

- all `68 / 68` exact indexed-vs-unindexed current-world frame pairs are non-byte-identical;
- `24..55` changed pixels per frame;
- maximum channel delta exactly `1` LSB;
- `0` pixels exceed `1` LSB;
- representative state `08` reproduces `55` changed pixels in `path_eye` and `24` in `elevated_oblique`, each with maximum `1` LSB.

Observed at normal and enlarged scale:

- the preferred planar manufactured read from Direction 020 remains unchanged to perception;
- no new hard-edge discontinuity appears on the galvanized frame;
- no highlight band visibly breaks, jumps, inverts or changes material hierarchy;
- no silhouette, hole, missing face, culling break, material-role collapse or state-dependent highlight pop appears;
- the sparse amplified differences sit on rasterized Building highlight/edge pixels and are not perceptible as an Art-level degradation at native retained scale.

Visual Observer / QA independently reports the same bounded non-corruption result over all 68 retained frames, including no hole, missing face, clipping, hard-edge discontinuity, material-role collapse or state highlight pop.

## Art Direction decision

### PASS — exact current Godot repack is visually non-degrading in retained views

**`PASS_ART_DIRECTION_BUILDING_PLANAR_ROLE_GODOT_NORMAL_REPACK_RETAINED_VIEW_NONDEGRADATION_021`**

Exact meaning:

> The exact current indexed planar-role Godot receiver, including its measured `120 / 1008` galvanized-frame decoded-normal repack, does not create an Art-significant visible degradation in the 68 retained `path_eye` / `elevated_oblique` current-world frames. Preserve Direction 020's planar-role visual preference and the fixed five-role material hierarchy.

This is an appearance PASS only. It does not establish mathematical normal equality, byte equivalence, arbitrary-camera safety, arbitrary-mesh safety, or transport exactness.

### HOLD — exactness remains a Technical-Art truth

**`HOLD_BUILDING_PLANAR_ROLE_NORMAL_TRANSPORT_EXACTNESS__MEASURED_REPACK_REMAINS_REAL`**

Technical Art must keep the exactness boundary visible. Art's bounded appearance PASS does not authorize changing the structural report to `exact`, weakening UC's conservative default, or silently treating the measured repack as universal tolerance policy.

## Default-adoption hold

**`HOLD_DEFAULT_BUILDING_PLANAR_ROLE_ADOPTION__RESIDUAL_PRIMITIVE_TARGET_DEVICE_AND_ENVIRONMENT_DECISION_PENDING`**

The prior QA and Art visual gates for this exact indexed receiver are now closed, but default adoption is still not an Art-only decision.

Still open:

1. Runtime owns the residual `+180` RenderingServer primitive count versus the active segmented rollback receiver and target-device characterization.
2. Environment owns the actual default/rollback adoption decision.
3. Runtime PR #41's newer direct-index construction candidate is a separate active lane and must prove its own exact A/B; it does not inherit this PASS if its produced arrays or raster result differ.
4. Technical Art retains the explicit mathematical normal-repack HOLD even though retained appearance is accepted.

## Handoffs

### Map Technical Art PR #40

Art accepts the exact measured Godot normal repack as **visually non-degrading only in the retained current-world views**. Keep `HOLD_GODOT_NORMAL_REPACK_EXACTNESS` truthful. Do not rewrite the result as exact normal transport and do not generalize this one receiver's visual tolerance into UC.

### Map Environment PR #24

Independent QA plus Art now pass retained appearance for the exact indexed planar-role receiver at `038925282240441c475651bdc3737d1749c31d06`. Environment may treat the QA/Art visual gate as closed for this exact receiver, while preserving the active segmented rollback until Runtime target-device/residual-primitive and Environment adoption decisions are resolved.

### Map Runtime PR #41

Use the exact accepted indexed receiver as the visual control for the direct-index preparation experiment. Keep source geometry, five material roles, scalars, world, cameras and Weather sequence fixed. If direct construction changes any produced position/normal/index result or raster output, return an exact A/B to Art/QA; do not inherit Direction 021 automatically. The failed current workflow is evidence, not a reason to retune Art-owned appearance.

### Building Hard Surface / Materials

No source representation or material retune is requested. The newly measured normal repack is not a reason to add bevels, subdivisions, rounded tube language, brighter steel, roughness compensation or lighting compensation.

## Explicit non-claims

This direction does **not** establish:

- exact decoded-normal transport through the current Godot index/commit path;
- a generic normal tolerance for UC or arbitrary assets;
- acceptance of Runtime PR #41's direct-index candidate;
- default Environment adoption;
- target-device CPU/GPU/FPS/VRAM/heap behavior;
- acceptability of the residual `+180` primitive count on target hardware;
- arbitrary cameras, FOVs, resolutions or renderers;
- UV/tangent/texture/normal-map/decal equivalence;
- collision/navigation/physics/manufacturing/gameplay validity;
- architectural correctness;
- automatic UC extraction or Profession Fabric promotion;
- CANON;
- production/game readiness;
- Building mastery;
- Technical-Art mastery;
- 3D Art Direction mastery.

## Four-root check

- **Truth:** the normal repack remains explicitly nonzero and non-exact even though its retained appearance passes.
- **Agency / non-domination:** Art decides only the bounded appearance question; Technical Art owns exactness, Runtime owns performance/preparation, and Environment owns adoption.
- **Continuity:** the active segmented rollback, unindexed planar-role, indexed planar-role and newer direct-index experiment remain separately named and rollbackable; Direction 020 remains intact rather than silently rewritten.
- **Wisdom before speed:** close the gate evidence actually resolves, but do not convert a tiny retained-view delta into a universal tolerance or default-adoption shortcut.

The four AXM roots remain the merge gate.
