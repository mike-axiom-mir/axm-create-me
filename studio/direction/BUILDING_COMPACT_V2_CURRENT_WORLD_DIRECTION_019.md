# Building Compact-v2 Current-World Direction 019

Date: 2026-09-17
Role: 3D Art Director
State: **PASS_ART_DIRECTION_BUILDING_COMPACT_V2_HARD_SURFACE_LOOK_PREFERENCE_019 / HOLD_EXACT_COMPACT_V2_CURRENT_WORLD_ADOPTION__RUNTIME_REGRESSION_AND_INDEPENDENT_QA_PENDING**

`axm-create-me` remains coordination only. This packet changes no Building, Map, Runtime, Materials, Geometry, Universal Creation, Profession Fabric, gameplay or product implementation.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

## Why this is the highest-leverage ready Art gate

Nature Direction 018 now has truthful wall-clock characterization, but Visual QA correctly blocks final timing/naturalness because Map VFX PR #35 retains no direct timed visual capture. Retiming or aesthetic acceptance there would outrun evidence.

Character remains structurally held by nonzero sampled shoulder intersections. Animal remains held on transported deformed direction-frame equivalence. Map Runtime #34's footprint cue trade remains one pixel / one LSB.

By contrast, Building now has an exact current-world receiver A/B with retained real Godot frames, a complete active-consumer Runtime comparison, and a materially visible fixed-view delta. That is a ready Art Direction decision surface.

## Exact reviewed identities

Current-world Environment owner:

- repository: `mike-axiom-mir/axm-map-design`;
- Environment PR #24 head: `ef2cb9cc84edc10ab66c2230daca625623e0b00d`;
- semantic Building source: `header-segmented-23`;
- active current-world receiver: `184v / 276t / 5 surfaces`;
- exact compact-v2 review receiver: `boundary-only-union-shell-conforming-compact-v2-001`, `1004v / 2052t / 5 surfaces`.

Compact-v2 donors remain separately owned:

- Building Geometry PR #8: `16253e7dd2f8cd590667f9631e4b50fdfcc7280d`;
- Building Hard Surface PR #9: `35d0ba62d7e534b3cd00ac69e99386843ffa3f2e`;
- Building Materials PR #3: `4179aa1401f5a9114399e2f998c96809d4b8ed2e`.

Hard Surface still classifies compact-v2 as an explicit non-default derived receiving option. No semantic-source replacement is implied.

Runtime current-world consumer gate:

- Map Runtime PR #36 exact head: `99ec4d0caac25cf52e9b20089ecc4cf8ef8dee5a`;
- exact parent artifact: workflow `35179857526`, artifact `10478624997`;
- exact compact-v2 Environment artifact: workflow `35179857530`, artifact `10480305129`.

The two downloaded archives were independently rehashed during this Art pass:

- parent archive SHA-256: `064388110840c8034fe359bd47db1ceacfb3ed9ade3e56bb03e3210b49de761f`;
- compact-v2 archive SHA-256: `2d1b747ae9eacf9ee0eb3754ba8fcd27f367e3edf77c3f6a66d700871ff83b94`.

These are hashes of the exact downloaded ZIP bytes used for this review; no claim is made that they are a separately published GitHub digest field.

## Exact retained current-world delta

Independent pixel comparison reproduced the Runtime/Environment measurements across the retained current-world sequence.

For the source-width candidate presentation:

- `path_eye`: exactly `25,376 / 792,000 = 3.204040404%` pixels differ by more than 1 LSB in every reviewed state; fixed Building-local bbox `x=346..753, y=256..437`; maximum channel delta `198` LSB;
- `elevated_oblique`: about `19,113..19,117 / 792,000 = ~2.413%` pixels differ by more than 1 LSB; fixed Building-local bbox `x=594..888, y=171..365`; maximum channel delta `180` LSB.

The same Building-local difference persists under the Weather control presentation. This is therefore not a Weather-width effect and not a tiny raster-noise event.

Runtime PR #36 also measures the exact compact-v2 candidate against the actual active Map receiver, not the much larger Building boundary-shell reference:

- `+106,560 B` proof-host buffer memory;
- `+5,328` RenderingServer primitives;
- `+0` draw calls;
- `+0` objects;
- `+0 B` observed texture memory.

Those counters are proof-host characterization, not target-device certification.

## Direct Art Direction observation

I directly inspected enlarged current-vs-compact frames at representative states in both fixed cameras and isolated the Building region.

The compact-v2 image is **not visually neutral**, but the change is directionally useful in these exact views:

- the galvanized front posts and upper header read as a more coherent manufactured hard-surface system;
- highlight flow is less dominated by the segmented receiver's locally bulged / broken bright bands at box boundaries;
- vertical structural rhythm reads straighter and more even;
- the accepted material hierarchy remains intact: galvanized frame first, readable but subordinate infill, darker roof, ochre service surface as the warm accent;
- the pavilion silhouette, opening layout and broader world hierarchy remain recognizably the same;
- no material scalar, lighting, camera, Weather, Nature, Object or path change is needed to obtain the cleaner hard-surface read.

This is a **fixed-view visual preference**, not a claim that compact-v2 is the right production representation.

## Art Direction decision

**`PASS_ART_DIRECTION_BUILDING_COMPACT_V2_HARD_SURFACE_LOOK_PREFERENCE_019`**

Exact meaning:

> In the exact retained current-world `path_eye` and `elevated_oblique` views, prefer the compact-v2 receiver's cleaner planar / structural highlight language over the active segmented receiver's more broken galvanised highlight response. Preserve the existing five-family material hierarchy and do not compensate through lighting, material retuning, atmosphere, camera or unrelated world changes.

This chooses a **visual target**, not a mesh implementation.

## Adoption hold

**`HOLD_EXACT_COMPACT_V2_CURRENT_WORLD_ADOPTION__RUNTIME_REGRESSION_AND_INDEPENDENT_QA_PENDING`**

The exact `1004v / 2052t` receiver is not approved as the current Map representation by this visual preference alone. Against the actual active `184v / 276t` receiver it is a real proof-host cost increase, while independent Visual QA has not yet reviewed this exact 68-frame Building A/B.

The smallest next cross-owner question is therefore:

> Can Building / Environment preserve the preferred cleaner hard-surface appearance without silently accepting this exact current-world representation cost, or provide a clearly justified trade if no cheaper equivalent exists?

Art Direction does not prescribe the topology solution. Geometry, Hard Surface, Materials, Technical Art and Runtime retain implementation authority.

## Handoffs

### Map Environment PR #24

Keep the exact failed compact-v2 review evidence. Do not relabel the inherited `0.1%` continuity-gate failure as aesthetic failure: the visual delta is real and Art-preferred in these fixed views. Also do not adopt compact-v2 automatically. Preserve the current segmented receiver as the active rollback baseline until the adoption hold is resolved.

### Map Runtime PR #36

Runtime's active-consumer comparison is accepted as the correct cost boundary. Art preference does not override the measured `+106,560 B / +5,328 primitives` proof-host regression, and Runtime should not be asked to weaken its gate to accommodate the look.

### Building Geometry PR #8 / Hard Surface PR #9

No rollback of compact-v2 is requested. Keep it as a truthful source-owned derived receiving option. If a lower-cost receiver can reproduce the preferred planar/highlight response while preserving source ownership, treat that as a new exact candidate rather than silently mutating compact-v2 or the current receiver.

### Building Materials PR #3

Keep the existing five-surface family fixed. The isolated reference-boundary-shell vs compact-v2 Materials proof already shows no scalar retune is needed under the explicit hard-normal path. Do not hide receiver differences with color/roughness changes.

### Visual Observer / QA

Independently inspect the exact parent/compact 68-frame current-world set for hard-edge artifacts, highlight popping, holes, silhouette drift, material-role loss or camera-specific regressions. Do not reduce the review to the `0.1%` numeric guard; the Art question is whether the preferred hard-surface read survives the full sequence without new defects.

### Technical Art / Runtime / UC / Profession Fabric

No transport, device-performance or generic extraction result transfers from this direction. One Building representation conflict is not evidence for a universal simplifier or receiver policy.

## Explicit non-claims

This direction does **not** establish:

- compact-v2 as semantic source or default receiver;
- current-world compact-v2 runtime adoption;
- independent QA acceptance;
- arbitrary camera/FOV/resolution/lighting/render-path equivalence;
- smooth/generated-normal, tangent, UV, texture or normal-map equivalence;
- GLB/import/export transport equivalence;
- target-device CPU/GPU/FPS/VRAM/heap/thermal/battery acceptance;
- architectural, manufacturing or physical validity;
- collision, navigation, interaction or gameplay behavior;
- final Nature flutter timing/naturalness;
- Animal direction-frame transport resolution;
- Character shoulder-intersection freedom;
- CANON;
- production/game readiness;
- Building mastery;
- 3D Art Direction mastery.

## Four-root merge gate

- **Truth:** a 2.4–3.2% fixed-view raster change is reported as a real visual change, not mislabeled as continuity; the proof-host cost regression is kept equally visible.
- **Agency / non-domination:** Art Direction selects the look target only. Environment owns composition, Geometry/Hard Surface own receiving representations, Materials owns surface values, QA owns independent verification, Runtime owns performance, and Technical Art owns transport.
- **Continuity:** the active `184v / 276t` receiver stays as rollback baseline; compact-v2 remains separately addressable and non-default; no prior evidence is silently rewritten.
- **Wisdom before speed:** preserve the cleaner visual target without spending a substantially larger current-world representation budget merely because a source-relative candidate is named `compact`.

The four AXM roots remain the merge gate.
