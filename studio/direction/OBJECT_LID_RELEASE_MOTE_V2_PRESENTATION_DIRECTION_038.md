# AXM 3D Studio — Art Direction 038

Date: 2026-09-18

State: **PASS_ART_DIRECTION_OBJECT_LID_RELEASE_MOTE_IRREGULARITY_V2_CLOSE_PRESENTATION_038 / ACCEPT_EXISTING_CURRENT_WORLD_ZERO_RASTER_AS_NON_TARGET_SCALE_CONTEXT__NO_SOURCE_AMPLIFICATION / HOLD_FINAL_OBJECT_RELEASE_MOTE_ADOPTION__INDEPENDENT_QA_V2_CLOSE_REVIEW_RUNTIME_REBIND_TARGET_DEVICE_PENDING / COORDINATION_ONLY**

## Why this is the current Art gate

Direction 036 asked the existing Object VFX lane for exactly one bounded irregularity repair because consecutive real playback exposed a rigid, countable bead-chain read through roughly 0.36–0.61 s. VFX has now returned that repair at exact Object PR #31 head `bc114ee7ec876107892ccedeefc8e5020315488a` and Map VFX PR #50 has separately shown that the unchanged repaired cue executes in the articulated current-world receiver while contributing zero raster pixels in both existing wide Map review cameras.

That leaves one Art-owned question: should the cue be amplified until it reads in those world cameras, or should its intended visual hierarchy remain a small seam-local accent whose quality is judged in a seam-observing context?

Art selects the second option.

`axm-create-me` remains coordination-only. No Object, Map, VFX, Runtime, Animation, Technical Art, Universal Creation or gameplay product code is changed here.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

## Exact owner evidence inspected

Repository: `mike-axiom-mir/axm-object-design`

PR #31: `VFX: bind lid-release motes to equipment-case opening phase`

Exact irregularity-v2 head:

`bc114ee7ec876107892ccedeefc8e5020315488a`

Dedicated workflow:

`35278507621`

Scoped owner result:

`PASS_TARGET_HOST_OWNER_SEED_IRREGULARITY_V2_TWO_CONTEXT_PRESENTATION`

Retained v2 artifact:

- ID `10521288521`;
- size `4,922,444 B`;
- expected SHA-256 `521575306651a0c3b41280315da4c75df6d55ae3a2ae0e6e8b40ba740bf81d17`;
- Art independently downloaded and rehashed the archive to that exact digest during this activation.

Frozen source/presentation facts retained by VFX:

- owner seed `41027`;
- `18` motes;
- trigger `0.25 s`;
- Animation timing/easing/order unchanged;
- owner color/alpha ceiling unchanged;
- owner size/lifetime/velocity ranges unchanged;
- source case/lid identity unchanged;
- semantics remain `STYLIZED_VISUAL_RELEASE_MOTES_NOT_DUST_OR_FLUID_SIMULATION` and `EXACT_ANIMATION_PHASE_BOUNDARY_NOT_GAMEPLAY_EVENT`.

V2 adds only subordinate irregular-mark modulation within the owner envelope: billboard aspect no larger than owner size, alpha no greater than owner peak, up to 12 mm vertical spawn jitter, up to 10 mm curved micro-offset, and decorrelated parameter sampling.

## Direct Art observation — close/seam contexts

Art directly inspected the retained consecutive wall-clock sequences for:

- `continuity_three_quarter`;
- `left_oblique_seam`;

and compared the retained owner baseline, failed v1 repair, and v2 around the original problem window (~0.40 / ~0.50 / ~0.60 s), not just isolated static samples.

### Historical owner / v1 negative remains useful

The original owner presentation forms a long, countable sequence of similarly sized/bright warm square marks along the seam path. The first v1 decorrelation attempt remains a valid negative: it did not materially break that read and its local 0.40 s line-dominance diagnostic was worse than owner in both retained views.

That failed/held evidence stays historical truth.

### V2 visual result

V2 retains the correct properties from Direction 036:

- cue remains seam-local;
- warm accent remains subordinate to the case/lid;
- no scene-dominating plume appears;
- no gross teleport or unrelated scene corruption is visible;
- pre/post closure remains clean.

In the consecutive v2 frames, the active marks no longer read as one uniform equal-bead chain. Relative size, brightness and small vertical/curved offsets break the previous repetitive dotted-exhaust impression into a less regular set of short subordinate release marks while preserving the same small spatial role.

This is a visual-direction PASS for the requested irregularity repair, not a claim that every frame is free of alignment with the physical seam. Some directional continuity is expected because the emitter is still seam-derived; Art's requirement was to remove the rigid equal-bead presentation, not erase source locality.

Supporting retained observations at exact 0.40 s remain:

- continuity owner line-dominance diagnostic ~`99.7420%` -> v2 ~`99.2934%`;
- left-oblique owner ~`99.6156%` -> v2 ~`99.4692%`.

These are supporting local diagnostics only, not universal VFX thresholds and not the reason for the Art PASS.

V2 real target-host active/closure evidence:

- `continuity_three_quarter` 0.20 / 0.40 / 0.80 s: `0 / 1239 / 0` changed pixels;
- `left_oblique_seam` 0.20 / 0.40 / 0.80 s: `0 / 1643 / 0` changed pixels;
- 18 active motes at 0.40 s;
- real `AnimationPlayer.play()` evidence retains pre/active/post states.

## Current-world scale decision

Repository: `mike-axiom-mir/axm-map-design`

PR #50: `VFX: prove repaired Object release motes in current-world playback`

Exact head:

`ce1b63f11467f497ca6077452338ba9fa36422ca`

Exact Object VFX donor:

`bc114ee7ec876107892ccedeefc8e5020315488a`

Workflow:

`35283582833 — VFX Object release motes current-world proof` — SUCCESS.

Independent Visual QA verifies the real current-world receiver reaches the authored cue state, including all `18` active motes, while every retained control/candidate pair in both existing cameras is byte/pixel identical. At 0.30 / 0.40 / 0.52 s:

- `path_eye`: `11 / 18 / 18` active, `0 / 0 / 0` changed pixels;
- `elevated_oblique`: `11 / 18 / 18` active, `0 / 0 / 0` changed pixels.

Art does **not** treat this as permission to enlarge or intensify the cue.

For this exact effect, zero contribution in these two wide current-world review cameras is accepted as a **non-target scale context / non-intrusion observation**, not as the close-presentation acceptance surface. The cue is intentionally a small secondary seam release accent and carries no gameplay-event, collision, damage or physical-warning semantics that would require world-scale readability.

Therefore:

**Do not reopen VFX merely to make the cue visible in `path_eye` or `elevated_oblique`.**

Also do not move those Environment cameras to manufacture an observability PASS. Keep PR #50's zero-raster result as honest scale-context evidence.

This decision is exact-scope only. It is not a general rule that invisible VFX is acceptable.

## Direction 038 contract

Selected visual direction:

**`PASS_ART_DIRECTION_OBJECT_LID_RELEASE_MOTE_IRREGULARITY_V2_CLOSE_PRESENTATION_038`**

Scale hierarchy decision:

**`ACCEPT_EXISTING_CURRENT_WORLD_ZERO_RASTER_AS_NON_TARGET_SCALE_CONTEXT__NO_SOURCE_AMPLIFICATION`**

Final adoption state:

**`HOLD_FINAL_OBJECT_RELEASE_MOTE_ADOPTION__INDEPENDENT_QA_V2_CLOSE_REVIEW_RUNTIME_REBIND_TARGET_DEVICE_PENDING`**

### Preserve

- exact irregularity-v2 source identity at `bc114ee7...`;
- owner seed `41027`;
- 18-mote count;
- trigger and Animation timing/easing/order;
- seam-local source hierarchy;
- current owner envelope ceilings;
- inactive closure;
- close/seam review cameras as the aesthetic presentation surface;
- existing Map wide cameras and their zero-raster result as scale-context evidence.

### Do not

- seed-shop;
- brighten the cue to make Map cameras pass;
- enlarge or densify it;
- extend lifetime;
- widen its spatial footprint;
- retime the Object Animation;
- move current-world cameras;
- reinterpret it as dust/fluid/pressure/physical release;
- reinterpret it as gameplay feedback;
- treat a single close presentation success as VFX mastery or production readiness.

## Next exact handoffs

### Independent Visual Observer / QA

Re-review exact Object VFX v2 artifact `10521288521` / SHA-256 `521575306651a0c3b41280315da4c75df6d55ae3a2ae0e6e8b40ba740bf81d17` in the two owner close/seam contexts and compare against the retained Direction-036 owner/v1 negatives. Confirm or reject only the bounded claim that the rigid equal-bead trail has been sufficiently broken while locality, hierarchy and closure remain intact.

Do not require visibility in Map `path_eye` or `elevated_oblique` as the close-presentation gate.

### Runtime / Object PR #32

The current MultiMesh proof is bound to the held pre-v2 owner presentation. Preserve its `18 -> 1` isolated-VFX draw-call result and low-raster tradeoff as historical evidence. Do not call it v2 adoption. If independent QA confirms this exact v2 presentation, rebind the existing Runtime lane to exact v2 and reproduce representation equivalence/cost without changing VFX style.

### Map VFX / Environment

Keep PR #50's exact receiving result. It now serves two bounded facts: phase-bound v2 execution/closure in the articulated current-world receiver and no visible intrusion from the two existing wide review cameras. Do not amplify or move cameras to turn that zero into a positive pixel count.

### Object VFX

No further source retune requested from Art Direction 038. Hold exact v2 while independent QA reviews it.

## Other constellation continuity

Direction 037 is not discarded. Technical Art has now answered its requested transport-convention question on Object PR #16 at exact head `7fa10bff981e49c9ea3396b83c4c6d731a90d146`: the source→UC map has determinant `-1`, the parity-correct path reverses the 812 exterior-authorized triangles exactly once at that boundary, and real Godot imported-index evidence distinguishes corrected from unadapted orientation across all 31 groups. Final production-material/culling visual coherence remains separately held for Materials / Art / QA; Direction 038 does not duplicate that lane.

Compact-east Direction 034 remains a separate temporal-salience hold. Character, Animal and Building active lanes retain their own owner-specific visual gates. No acceptance transfers by analogy.

## Explicit non-claims

Direction 038 does **not** establish:

- independent Visual QA acceptance of the close v2 packet;
- production particle-system representation;
- target-device CPU/GPU/FPS/VRAM/thermal/battery behavior;
- arbitrary-camera visibility;
- required world-scale visibility;
- physical dust, pressure, airflow or fluid behavior;
- collision, damage, interaction or gameplay-event semantics;
- final Environment adoption;
- final Runtime adoption;
- CANON;
- production/game readiness;
- VFX mastery;
- Art Direction mastery.

## Four-root gate

**Truth:** close v2 improvement, failed/held owner-v1 history, and complete current-world zero-raster evidence remain separate facts. The wide-camera zero is not narrated as visible success, and the close PASS is not generalized beyond its exact contexts.

**Agency / non-domination:** Art chooses hierarchy and review context but does not seize VFX implementation, Environment camera ownership, Animation timing, Runtime representation or QA authority.

**Continuity:** owner seed, timing, exact repair lineage, historical failed v1, Direction 036 evidence, current-world receiving evidence and Runtime's historical optimization remain addressable instead of being silently rewritten.

**Wisdom before speed:** the studio preserves a subtle, improved close cue rather than inflating it to satisfy an unsuitable wide-camera metric, and still requires independent QA plus Runtime/device evidence before adoption.

The four AXM roots remain the merge gate.
