# AXM 3D Art Direction — Direction 040

Date: 2026-09-18

State: **PASS_ART_DIRECTION_ANIMAL_RUNTIME_19_KEY_RETAINED_VIEW_TRADEOFF_040 / FREEZE_19_KEY_CANDIDATE_AS_RUNTIME_REVIEW_REFERENCE__DO_NOT_RETUNE_SOURCE_ANIMATION / HOLD_FINAL_ANIMAL_KEY_BUDGET_ADOPTION__INDEPENDENT_QA_WHOLE_ANIMAL_CONTINUOUS_MOTION_TARGET_DEVICE_PENDING**

## Scope

This packet records one bounded Art Direction decision on the exact Animal Runtime animation-key budget candidate in `mike-axiom-mir/axm-animal-design` PR #30.

`mike-axiom-mir/axm-create-me` remains coordination only. No Animal source mesh, rig, material, animation curve, Runtime implementation, target-host receiver, gameplay system or Universal Creation product code is modified here.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

## Why this lane now

The preceding Art activation closed the exact Object rigid-shell transport/culling visual blocker for the parity-corrected Technical-Art path. Fresh constellation state then exposed several downstream review candidates:

- Object VFX irregularity-v2 now has Art PASS and independent QA PASS; Runtime has already rebound its MultiMesh receiver to the exact v2 owner bytes and is waiting on independent Runtime-representation QA / target-device evidence. Art does not duplicate that active QA lane.
- Map Environment PR #49 has a +20 mm service-frame successor with compact A/B atlases ready for Art/QA. The delta is intentionally local and remains queued.
- Animal Runtime PR #30 contains a **measured nonzero silhouette/deformation tradeoff** caused by reducing the exact animation representation from 41 serialized keys to 19 while keeping the source-authored animation unchanged. Runtime explicitly returned this tradeoff to Art Direction / QA rather than auto-adopting it.

The Animal tradeoff is therefore the strongest fresh Art-owned decision that can be made without duplicating an already-active specialist lane.

## Exact evidence identity

Repository: `mike-axiom-mir/axm-animal-design`

PR: **#30 — `Runtime: reduce Animal animation key import budget`**

Exact Runtime head: `13ba20d198d2b7c5e428167745d59927b3084004`

Exact Animation owner head: `eb21e0e0fd888bbb5fa41c73a6c0f1c731f662c2`

Dedicated workflow: `35288829261 — Runtime Animal animation key budget evidence` — **SUCCESS**.

Retained artifact:

- ID `10525970648`;
- size `175,311 B`;
- SHA-256 `ad071f58796b606d707168af9619d988a497ba1a745dda8ac62b42e7f814b996`.

Art independently downloaded the archive this activation, reproduced the exact byte size and rehashed it to the exact same SHA-256 before judging the images.

Runtime representation result:

- serialized rotation keys: `41 -> 19`;
- removed keys: `22 / 53.6585366%`;
- animation accessor bytes: `820 -> 380 B` (`440 B / 53.6585366%` saved);
- complete GLB: `10,296 -> 9,852 B` (`444 B / 4.3123543%` saved);
- candidate SHA-256: `a8a32b58ad3bad44176a676b00f5cf1c20d1a2ec6da275b683d8f73a69088d6b`;
- dense diagnostic sampling: `321` samples / `320 Hz`;
- maximum quaternion residual: `0.0547204398°`;
- mean quaternion residual: `0.0176527597°`;
- worst diagnostic time: `0.475 s`;
- authored `0.50 s` peak remains an explicitly retained source key;
- source animation retimed: `false`;
- source-authored keys modified: `false`.

Real Godot 4.7.2 / GL Compatibility / llvmpipe receiver evidence retains `80` triangles, unchanged material bindings and a maximum retained pose-bounds delta of `0.0004068017 m` (~0.407 mm).

## Direct Art observations

Art inspected all six retained visual images: three shaded frames plus their three coverage masks.

### Worst-time view 00 — acceptable bounded drift

At `0.475 s`:

- raw changed pixels: `9,850`;
- pixels >1 LSB: `152`;
- coverage-mask changes: `59` pixels;
- maximum RGB-channel delta reaches `244 LSB` where the tiny silhouette shift swaps foreground/background coverage.

Native and enlarged comparison shows a very small edge/silhouette displacement along the lower bend and outer side. The form remains continuous and readable. No joint inversion, detached segment, gross volume collapse, obvious shading discontinuity or changed motion phase is visible.

### Worst-time view 01 — acceptable bounded drift

At the same `0.475 s` witness:

- raw changed pixels: `8,967`;
- pixels >1 LSB: `564`;
- coverage-mask changes: `81` pixels;
- maximum RGB-channel delta again reaches `244 LSB` at changed coverage edges.

This alternate view makes the tiny silhouette change easier to locate, but it still reads as a sub-pixel/few-pixel pose approximation rather than a changed animation idea. The bend, taper, lower flare and overall pose hierarchy remain coherent.

### Retained authored peak — exact visual identity

At `0.50 s`, the shaded control and candidate are byte-identical, and their coverage masks are also byte-identical:

- raw changed pixels: `0`;
- pixels >1 LSB: `0`;
- coverage changes: `0`;
- maximum channel delta: `0`.

This matters because the peak pose is intentionally kept as a source-authored key rather than approximated away.

## Art Direction decision

**`PASS_ART_DIRECTION_ANIMAL_RUNTIME_19_KEY_RETAINED_VIEW_TRADEOFF_040`**

For the exact retained simplified Animal deformation proof surface, the 19-key Runtime candidate preserves the intended pose read strongly enough that the measured worst-time drift is acceptable as a **bounded Runtime representation tradeoff**.

The candidate may be retained as the Runtime review reference for the next receiving gates.

This is **not** permission to delete or rewrite the 41-key authored source animation. It is also not permission to reduce below 19 keys, widen the `0.075°` reducer tolerance, change interpolation, retime the clip, alter rig weights or change materials in order to chase additional storage savings.

Reference policy:

**`FREEZE_19_KEY_CANDIDATE_AS_RUNTIME_REVIEW_REFERENCE__DO_NOT_RETUNE_SOURCE_ANIMATION`**

Final adoption state:

**`HOLD_FINAL_ANIMAL_KEY_BUDGET_ADOPTION__INDEPENDENT_QA_WHOLE_ANIMAL_CONTINUOUS_MOTION_TARGET_DEVICE_PENDING`**

## Handoffs

### Runtime / Optimization — Animal PR #30

Keep exact candidate head `13ba20d...`, the 19-key representation, current reducer bound and retained 41-key control as the A/B reference. Do not squeeze the channel further from this Art PASS.

This Art result accepts only the current retained-view visual tradeoff. Runtime/device performance and production representation remain Runtime-owned.

### Independent Visual Observer / QA

Review the exact `10525970648` artifact independently. Preserve the exact worst-time `0.475 s` and exact authored `0.50 s` peak controls. Art does not ask QA to inherit this PASS.

A useful QA discriminator is whether the 19-key candidate introduces any coherent pose/silhouette defect beyond the already-recorded tiny edge drift in the two worst-time views.

### Animation

No retime, interpolation change, easing change or authored-key rewrite is requested. The 41-key owner animation remains the animation truth/source; the 19-key file is only a Runtime receiving representation.

### Rigging / Materials / Technical Art

No Art-requested source, deformation, shading or transport mutation follows from this decision. Any future whole-animal/current-world receiver must prove its own continuity; this simplified proof does not transfer automatically.

## What remains required before final adoption

- independent Visual QA on the exact 19-key retained packet;
- materially broader whole-animal/current-world visual evidence rather than only the simplified deformation proof body;
- continuous playback evidence sufficient to reveal rhythm/stepping regressions, not only static exact-time samples;
- target-device/import/runtime measurements where an honest device surface exists;
- preservation of the source-authored 41-key animation and rollback path.

## Explicit non-claims

Direction 040 does **not** establish:

- that 19 keys are universally optimal for Animal clips;
- that `0.075°` is a universal visual tolerance;
- arbitrary-camera equivalence;
- whole-animal visual acceptance;
- final deformation/tangent-space/material quality;
- continuous-motion naturalness or lack of stepping;
- target-device CPU/GPU/FPS/VRAM/import-time/thermal improvement;
- gameplay/controller acceptance;
- permission to alter the authored 41-key source;
- automatic use of the reducer on other characters, animals or clips;
- CANON;
- production/game readiness;
- Runtime mastery;
- Animal mastery;
- Art Direction mastery.

## Continuity

Direction 039 remains intact: the exact Object parity-corrected Technical-Art shell path is the retained material/cull visual reference and the unadapted path remains a preserved negative. Direction 038's Object VFX irregularity-v2 close-presentation PASS also remains intact; independent QA has now agreed with it, while Runtime v2 representation QA/device evidence remains separate.

Direction 040 does not transfer Object decisions into Animal or vice versa.

## Four-root gate

**Truth:** the nonzero silhouette drift is retained rather than hidden behind an aggregate residual. The authored peak's byte-identical frame is recorded separately. This PASS is bounded to exact bytes, three retained views and the current reducer candidate.

**Agency / non-domination:** Art judges the visible tradeoff but does not seize Animation source authority, Runtime implementation/device authority, Rigging deformation authority or QA independence.

**Continuity:** the 41-key authored control remains authoritative and recoverable. The 19-key candidate is a separate receiving representation with exact provenance and rollback.

**Wisdom before speed:** a 53.7% key-count reduction is accepted only after direct worst-case visual review, while further compression and production adoption are explicitly stopped until broader visual/QA/device evidence exists.

The four AXM roots remain the merge gate.
