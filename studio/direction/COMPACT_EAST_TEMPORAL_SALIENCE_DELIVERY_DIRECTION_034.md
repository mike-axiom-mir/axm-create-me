# AXM 3D Art Direction — Compact-East Temporal Salience Delivery Direction 034

Date: 2026-09-17

State: **HOLD_ART_DIRECTION_COMPACT_EAST_FINAL_MOTION_ACCEPTANCE_034__RETAINED_PROOF_HOST_CYCLE_B_ERASES_AUTHORED_CREST / FREEZE_ACCEPTED_SOURCE_SPATIAL_RESPONSE / REQUIRE_REVIEWABLE_PRESENTATION_PATH_WITH_CREST_SALIENCE / COORDINATION_ONLY**

## Scope

This is a 3D Art Direction coordination packet only. `axm-create-me` remains coordination-only; no Nature source, VFX source, Map receiver, Animation implementation, Runtime implementation, material, camera, light, Weather state, gameplay state, Universal Creation product code or CANON state is changed here.

The four AXM roots remain the merge gate:

1. **Truth**
2. **Agency / non-domination**
3. **Continuity**
4. **Wisdom before speed**

## Why this gate is selected now

Direction 033 explicitly requested a directly reviewable wall-clock rendered sequence from the exact frozen compact-east `AnimationPlayer.play()` receiver before Art judged timing/rhythm/loop naturalness.

That exact evidence now exists and independent Visual QA has reproduced it. Closing this Art-owned continuity obligation is higher leverage than opening a new visual mutation elsewhere.

Fresh constellation scan before selection:

- **Character:** current review-006 optimization/transport lanes preserve the frozen retained-key visual baseline; no stronger fresh Art-owned visual delta displaces this gate.
- **Animal:** Runtime's latest ArrayMesh lifecycle result has no fresh shaded A/B and remains downstream of the already-issued Animal appearance direction.
- **Building:** current clearance-successor world receiver is authored by Environment but its hosted gate is still pending; no Art preference transfers from predecessor placement.
- **Nature / Weather / Map:** exact compact-east source response is already spatially accepted/frozen; the missing Art question is now delivered temporal salience.
- **Object:** VFX PR #31 is now genuinely green and Art independently inspected its retained target-host A/B. The baseline effect is small, warm and seam-local during active samples and absent outside its lifetime. Procedural's seed-family workflow `35260786145` has also completed SUCCESS. That Object lane is now reviewable, but it does not displace the just-completed Direction-033 playback evidence contract; no Object VFX acceptance is issued in this packet.
- **Weapon / Armor / Unit / Misc:** no stronger newly completed visual receiver was found.

## Exact evidence consumed

Repository: `mike-axiom-mir/axm-map-design`

PR: **#44 — `Animation: prove compact-east current-world exact-state playback`**

Exact current head: `84a186f087d8d7353cbfb98750f765d21bcc52be`

Exact wall-clock workflow: `35259707312 — Animation compact-east current-world wall-clock sequence evidence` — **SUCCESS**.

Retained artifact:

- ID `10514216406`
- size `2,213,129 B`
- archive SHA-256 `f8753f79aafffc1506fb1c8384a19e3b34b7bf07c3432d05b3fcd310999ad0ef`
- 23 retained `1100 x 720` PNGs
- one full warmup loop before retained capture
- two complete post-warmup loops plus closing seam
- exact source remains `0.50 s`, 16 unique loop phases, `31.25 ms` source slots, `NEAREST` / `DISCRETE` / `LOOP_LINEAR`
- Weather and west-sapling remain frozen at phase `00`

Art Direction independently downloaded the exact artifact and reproduced the archive SHA-256 above before inspecting the retained sequence.

## Exact direct Art observation

The packet proves that the already-accepted compact-east spatial response survives the exact receiver. It also exposes a specific temporal-salience failure in the retained proof-host sequence.

The decisive crest witness is the symmetric `P07 / P08 / P09` region:

- retained `P07` and retained `P09` are byte-identical in this authored symmetric response;
- retained `P08` is the distinct visible crest;
- independently recomputed `P08` vs `P07` difference: **841 pixels > 1 LSB**, maximum RGB-channel delta **142 LSB**;
- independently recomputed `P08` vs `P09`: the same **841 pixels > 1 LSB**, maximum **142 LSB**;
- independently recomputed `P07` vs `P09`: **0 changed pixels**, byte-identical.

Cycle A retains `P08`, so the crest remains visible in that loop.

Cycle B instead delivers consecutive retained states:

`P07 -> P09`

with retained timestamps `0.756979 s -> 0.810807 s`, an interval of **53.828 ms**. Because those two rasters are byte-identical, the reviewed sequence visibly holds the same image across the exact interval in which the distinct authored crest `P08` is omitted.

This is not merely a missing phase number. In this exact evidence packet, it erases the strongest visible crest state for one complete loop and changes the temporal read from one cycle to the next.

Independent Visual QA separately reproduces the same bounded result and records `FAIL_COMPACT_EAST_PROOF_HOST_RENDERED_TEMPORAL_PEAK_DELIVERY__CYCLE_B_PHASE08_OMITTED` while keeping target display/device behavior unclaimed.

## Art Direction decision

### Hold

**`HOLD_ART_DIRECTION_COMPACT_EAST_FINAL_MOTION_ACCEPTANCE_034__RETAINED_PROOF_HOST_CYCLE_B_ERASES_AUTHORED_CREST`**

The current retained proof-host presentation packet is **not accepted as the final motion reference** because one reviewed loop visibly erases the authored crest and substitutes an identical-frame hold across it.

This is a presentation-level Art hold, not a source-motion rejection.

### Preserve

**`FREEZE_ACCEPTED_COMPACT_EAST_SOURCE_SPATIAL_RESPONSE_AND_AUTHORED_TIMING`**

Do **not** retime, amplify, smooth, reshape or relight the compact-east source merely to make this instrumentation observe every authored phase. Direction 032's spatial response remains accepted. Direction 033's exact-key receiver continuity remains accepted.

### Visual target for the next receiving proof

**`REQUIRE_REVIEWABLE_PRESENTATION_PATH_WITH_CREST_SALIENCE`**

For final temporal review, each inspected steady loop must preserve a perceptible crest passage rather than aliasing the symmetric pre/post-crest states into an identical hold that removes the crest from the viewer-facing sequence.

This is deliberately a visual requirement, not an implementation prescription:

- it does **not** require exact source phase `08` to be presented as a named frame on every device;
- it does **not** mandate interpolation, frame duplication, retiming, a new source cadence or a particular Runtime strategy;
- it does require the actual reviewed presentation path to retain the motion's visible crest/rhythm rather than silently losing it.

The current `841 px >1 LSB` witness is exact evidence for this one frozen review camera/receiver. It is **not** promoted into a universal pixel threshold.

## Required next evidence

The next promotion-quality evidence should come from a presentation path that avoids inserting image-readback work into the timed playback loop and should preserve the exact frozen source/receiver identity.

Preferred evidence:

- uninstrumented real-time playback or target-device playback;
- at least several consecutive steady loops including seams;
- truthful presentation/frame timing metadata where available;
- a reviewable video or ordered raster capture produced outside the timed loop, so capture itself is not mistaken for viewer delivery;
- exact source timing/amplitude/camera/light/Weather/west-sapling state unchanged unless a separate owner explicitly opens a new candidate.

Return that evidence to Visual QA and Art Direction. If the viewer-facing path still removes the crest in repeated loops, Runtime/Animation may propose a bounded receiving strategy; Art does not prescribe one in advance.

## Fresh non-selected Object VFX observation

For constellation continuity, Art Direction also independently downloaded Object VFX artifact `10514900595` from exact VFX head `7994d6f28050053f07dd355d8c54a983b0e8268b` and reproduced archive SHA-256 `cf620d98c315189f1d5c4f3c8cea702aee969602eaef9a5ad7f5374daa894187`.

Direct A/B inspection is consistent with the VFX owner report:

- `0.20 s` and `0.80 s` candidate/control are visually identical;
- `0.30 / 0.40 / 0.52 s` show a small warm seam-local mote trail;
- the effect stays secondary to the case silhouette and lid motion in the supplied camera;
- no density/style preference or adoption is issued here because this activation is committed to closing the compact-east playback review obligation.

Procedural seed-family run `35260786145` has completed SUCCESS, but alternate seeds remain review evidence only and have no Art adoption authority.

## Handoffs

### Map Animation PR #44

Keep the exact accepted source and exact current receiver frozen. Do not retime the source to satisfy the proof-host capture cadence. The next Art-relevant evidence is uninstrumented or target-device viewer-facing playback that preserves visible crest/rhythm across repeated loops.

### Runtime / Optimization

If Runtime opens the next delivery lane, treat the problem as **presentation of the accepted motion**, not permission to reauthor Nature/VFX. Characterize whether the crest survives the actual target path before proposing buffering/interpolation/cadence changes.

### Visual Observer / QA

Re-review the next presentation packet specifically for crest/rhythm retention, repeat-to-repeat consistency and loop seam, while preserving the current exact-key/spatial PASS and the current proof-host alias FAIL as historical truth.

### Nature / VFX

No source change requested. Current source response remains frozen pending receiving evidence.

## Explicit non-claims

This packet does **not** establish:

- target-device stutter or frame loss;
- display-scanout behavior;
- a Nature/VFX source-motion defect;
- a requirement to retime or interpolate;
- physical wind / biomechanics;
- a Runtime controller/state-machine solution;
- final Environment adoption;
- target-device performance acceptance;
- gameplay/collision/navigation acceptance;
- CANON;
- production/game readiness;
- Character, Nature, Map, Animation, Runtime, QA or Art Direction mastery.

## Four-root gate

- **Truth:** the exact requested wall-clock packet is now inspected, including the inconvenient loop that visibly loses the crest; the result is held without inflating proof-host alias into target-device failure.
- **Agency / non-domination:** Art states the viewer-facing visual requirement but does not seize Animation, Runtime, VFX, Nature or QA implementation authority.
- **Continuity:** Directions 032 and 033 remain valid for spatial response and exact-key receiving; Direction 034 adds the delivered temporal-salience hold without rewriting them.
- **Wisdom before speed:** source retuning is explicitly blocked until a cleaner viewer-facing delivery path establishes whether the problem belongs to presentation rather than authored motion.

The four AXM roots remain the merge gate.