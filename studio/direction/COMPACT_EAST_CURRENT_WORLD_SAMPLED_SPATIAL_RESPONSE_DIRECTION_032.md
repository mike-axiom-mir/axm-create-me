# AXM 3D Studio — Art Direction 032

Date: 2026-09-17

Decision: **PASS_ART_DIRECTION_COMPACT_EAST_CURRENT_WORLD_SAMPLED_SPATIAL_RESPONSE_DIRECTION_032**

Companion hold: **HOLD_FINAL_COMPACT_EAST_MOTION_NATURALNESS_AND_ENVIRONMENT_ADOPTION__CURRENT_WORLD_PLAYBACK_PRODUCT_CAMERA_RUNTIME_PENDING**

## Scope

This is one bounded Art Direction decision over the exact current-world sampled compact-east Nature response. It does not edit product code or product assets. `axm-create-me` remains coordination-only.

The four AXM roots remain the internal merge gate: **Truth; Agency / non-domination; Continuity; Wisdom before speed**.

## Why this is the highest-leverage fresh Art gate

A fresh full-constellation scan found:

- Animal direction-frame appearance is already direction-frozen by Direction 031 and has moved into active Technical-Art/Runtime receiver work; another Art mutation there would duplicate live lanes.
- Character's newest optimization evidence is visually byte-identical under the retained Direction-030 setup; its larger remaining gates are continuous/full-body/tangent/device receiving.
- Building has a fresh Materials UV-density review, but Hard-Surface PR #17 has also corrected the utility-panel source standoff and explicitly requires downstream successor rebinding rather than silent PASS transfer. That chain is not the cleanest current Art adoption surface.
- Object roughness/UV appearance has existing Art direction; current work is mainly transport/storage/runtime follow-through.
- Weapon, Armor, Unit and Misc expose no stronger open implementation-backed visual gate.
- Nature/Map now expose something Art specifically owns: exact real-Godot sampled current-world readability, independent Visual-QA reproduction, and a newly-green combined Environment receiver that preserves the already-selected Object roughness candidate while stacking only compact-east response.

Therefore this activation chooses the compact-east **sampled spatial response** and deliberately does not retime it, rewrite it, or pretend sampled states prove delivered motion naturalness.

## Exact evidence consumed

### Current-world VFX receiver

Repository: `mike-axiom-mir/axm-map-design`

Map VFX PR #43: `VFX: receive compact-east Nature response in current world`

Current review head: `c7767c45cb24488f63de9a598f753d94b9141286`

Exact real-Godot receiving authority remains: `29ef2d4cc4398b3f26290e4e1f1f10398ca9898c`

Nature VFX owner head: `cef2ad78d8e36a55ada5dad07329f1a7125d48de`

Exact source response:

- 17 endpoint-inclusive states over `0.50 s`;
- 16 intervals at `31.25 ms` source spacing;
- exact neutral endpoints at phases `00` and `16`;
- source-local peak displacement ceiling `0.135 m`;
- Weather semantics remain `VISUAL_DIRECTION_ONLY_NOT_PHYSICAL_WIND_SPEED`.

The current-world receiving proof is green and preserves Weather, west-sapling motion, Building, Object, route, cameras and lighting.

### Parent-isolated readability evidence

VFX readability workflow: `35248021257` — **SUCCESS**.

VFX readability artifact: `10508665125`, SHA-256 `7c2081aa5fd439c74fdea7ba0828ed62377f1877bf793898a01bbed1c671f3a5`.

Independent Visual-QA readability artifact inspected by Art Direction:

- artifact `10508244231`;
- archive SHA-256 `229f1c9674c897c1107bbf11f6bd6116f1937e40c96e8aa19a13772e93c39d4a`;
- Art Direction independently downloaded and rehashed the archive to the same exact digest.

Independent QA reproduces:

- `elevated_oblique`: phases `01..15` are visibly different from the exact parent in both retained Weather modes;
- phases `00` and `16`: `0` changed pixels;
- peak phase `08`: `7,440` changed pixels, `7,240` pixels above 1 LSB, maximum `191 LSB`;
- maximum changed-frame share: approximately `0.939394%` of the `1100 x 720` frame;
- all effect deltas remain inside the fixed compact-east review envelope `x=594..939, y=287..671`;
- `path_eye`: exact non-observing context, `34/34` retained frames byte-identical to the parent.

### Exact selected-roughness + compact-east current-world coexistence

Map Environment PR #24 exact current head: `4bd7eaf6970716dde4159448c92556785f47e954`.

Dedicated Environment workflow:

- push run `35248195715` — **SUCCESS**;
- PR run `35248202905` — **SUCCESS**.

Retained combined artifact:

- artifact `10509037278`;
- size `7,184,621 B`;
- SHA-256 `8f2f8aa4bb11e2f868a6ce36dd381933ba1ea6c59be7b82ed00d1dfe5402ee97`;
- Art Direction independently downloaded and rehashed the archive to the same exact digest.

Exact combined state: `PASS_CURRENT_WORLD_SELECTED_ROUGHNESS_PLUS_COMPACT_EAST_COMPOSITION_REVIEW_READY__DUAL_ADOPTION_HELD`.

The combined receiver proves:

- `68` matched current-world frames across `17` states;
- exact selected-roughness runtime observation preserved in `17/17` states;
- exact selected roughness PNG SHA-256 `57cf746a9a7e0615884fe3c45c6c4df677c2bd0631def61b3ccb1684daa26949`;
- exact scalar R8 SHA-256 `b8d13c07f9b71278042b0d42d44b84579a3f327c6adf6723cae4c8c8f06dd38e`;
- compact-east exact phase sequence `0..16` preserved;
- neutral endpoint delta `0`;
- draw calls, objects, primitives, buffer memory and texture memory have `0` delta versus the exact selected-roughness parent in this proof receiver;
- both Environment adoption flags remain false.

This is coexistence evidence. It does **not** re-review Object roughness at distant world scale; the prior close-range Art roughness decision remains the appropriate appearance authority for that surface.

## Direct Art observation

Art Direction directly inspected the exact QA motion strips and the exact combined Environment current-world frames, including phases `00`, `08` and `16` in `elevated_oblique` and the declared non-observing `path_eye` context.

Within the retained observing view:

- the compact-east lower trunk/root read stays visually anchored while upper trunk/branch/leaf groups progressively displace toward the midpoint and return;
- the sampled branch hierarchy stays legible through peak response;
- the effect is clearly readable when compared across phases but remains subordinate to the broader pavilion/route/Object composition rather than becoming the dominant scene motion;
- the peak sampled silhouette does not show a gross detached branch mass, hole, broad collapse or camera-edge clipping;
- the cast-shadow response changes coherently with the sampled tree state rather than remaining visibly frozen;
- the exact neutral appearance returns at phase `16`;
- both retained Weather review modes preserve essentially the same compact-east readability pattern.

`path_eye` is truthfully non-observing for this effect. Art does **not** request camera movement or VFX amplification merely to make every camera see every effect. A future product camera that is expected to communicate this motion must separately prove observability; that is not a reason to corrupt the current source response.

## Art Direction decision

**Accept the exact compact-east sampled spatial response as the current visual baseline for the next motion receiver.**

Freeze for the next proof:

- Nature VFX owner head `cef2ad78d8e36a55ada5dad07329f1a7125d48de`;
- compact-east source identity and migrated neutral mesh identity;
- exact `0.135 m` source-local peak envelope;
- exact 17-state source sequence and neutral endpoints;
- current-world placement and current observing-camera setup;
- existing Weather semantics;
- selected Object roughness identity when reviewing the combined Environment branch.

Do **not** retune amplitude, branch hierarchy, source geometry, Weather direction, selected Object roughness, camera, lighting or composition merely to obtain a playback PASS.

This direction means only that the **sampled spatial shape/envelope is good enough to preserve while timing/playback is proved**. It is not a final motion-naturalness verdict.

## Current playback blocker — preserve it as a receiving problem

Map Animation PR #44 current head: `abad9beeb57dc99ba60aad60d3fb38046395972e`.

Workflow `35248112953 — Animation compact-east current-world playback evidence` completed **FAILURE** at the real `AnimationPlayer` playback step.

The retained failure packet is explicit:

- state: `FAIL_INFRASTRUCTURE`;
- source motion truth remains `DISCRETE_EXACT_VFX_STATES_NOT_SMOOTH_INTERPOLATION`;
- promotion effect: `NONE`;
- first real cycle missed exact states `[0, 2, 3, 5, 6, 7, 9, 10, 11, 12, 14, 15]`;
- exact authored source response remains unchanged.

Art Direction interprets this as **no permission to retime or reshape the source**. Animation should repair/characterize the receiving/playback mechanism against the frozen source first. Once exact current-world playback is green, return a reviewable delivered-motion trace/sequence for actual timing/naturalness judgment.

## Handoffs

### Map VFX PR #43

Freeze the current compact-east source response and readability evidence. No VFX/source retune is requested by Art Direction. The sampled spatial direction is accepted for the next receiver proof.

### Map Environment PR #24

The green selected-roughness + compact-east composition may remain the exact combined review parent. Do not convert that coexistence PASS into Environment adoption yet; continuous delivered motion, relevant product-camera observability, Runtime/device evidence and final QA remain separate.

### Map Animation PR #44

Repair the exact-state current-world playback failure without changing the source response to make the test easier. The next Art-relevant evidence is delivered playback of this frozen spatial response, not a new response candidate.

## Explicit non-claims

Direction 032 does **not** establish:

- physical wind speed, force, turbulence or biomechanics;
- final motion timing, rhythm, naturalness or loop feel;
- smooth/interpolated motion preference;
- successful current-world `AnimationPlayer` playback;
- product-camera coverage or a requirement that every camera observe this effect;
- final leaf sidedness, leaf materials or production vegetation shading;
- target-device CPU/GPU/FPS/VRAM/thermal/battery acceptance;
- Environment adoption of compact-east response;
- Environment adoption of selected Object roughness;
- gameplay, collision, navigation or interaction behavior;
- arbitrary vegetation-response transfer;
- CANON;
- production/game readiness;
- Nature mastery;
- VFX mastery;
- Animation mastery;
- Environment mastery;
- Visual QA mastery;
- 3D Art Direction mastery.

One green sampled current-world response plus independent QA is enough to freeze a bounded spatial direction. It is not enough to claim final motion or mastery.

## Four-root gate

- **Truth:** sampled spatial appearance is separated from delivered playback; the current Animation failure remains explicit; exact artifacts/heads are retained and independently rehashed; non-observing `path_eye` is not relabelled as a visual PASS.
- **Agency / non-domination:** Nature/VFX keep source-response authority, Animation keeps playback, Environment keeps composition/adoption, Runtime keeps device cost, QA keeps independent verification, and Art owns only visual preference.
- **Continuity:** prior Object roughness direction, current-world parent, VFX source response, QA evidence and failed Animation playback remain recoverable rather than silently replaced.
- **Wisdom before speed:** freeze a visually adequate spatial response and fix the receiver before authoring another effect or tuning around an infrastructure/playback failure.

The four AXM roots remain the merge gate.
