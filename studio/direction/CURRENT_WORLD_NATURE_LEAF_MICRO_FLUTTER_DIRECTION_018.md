# Current-World Nature Leaf Micro-Flutter Direction 018

Date: 2026-09-17
Role: 3D Art Director
State: **PASS_ART_DIRECTION_CURRENT_WORLD_NATURE_LEAF_MICRO_FLUTTER_SAMPLED_SHAPE_PREFERENCE_018 / HOLD_FINAL_NATURE_FLUTTER_TIMING__WALL_CLOCK_PLAYBACK_SHADED_BACKFACE_AND_INDEPENDENT_QA_PENDING**

## Scope

This is a bounded visual-direction decision only.

`axm-create-me` is coordination only. This packet changes no Nature, Map, renderer, Runtime, Universal Creation or product implementation.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

## Why this gate now

Direction 017 selected the current-world Nature receiver policy `woody=CULL_BACK / foliage=CULL_DISABLED` while explicitly holding flutter.

Map Environment PR #24 now has a completed exact current-world Godot receiver for the already-authored Nature VFX leaf-local flutter on top of that exact Direction-017 parent. The VFX source lane also has an exact-head green bounded candidate/review surface. This closes the missing Art-facing spatial-motion question without duplicating Geometry, Materials, VFX, Environment, Runtime or QA implementation ownership.

Other active lanes do not displace this gate:

- Animal production tangent adoption remains blocked by transported deformed direction-frame equivalence.
- Character shoulder structure still carries nonzero sampled self-intersections.
- Map Runtime's footprint indexing trade remains only a one-pixel / one-channel-LSB class difference.
- Building compaction is structurally strong but remains a separate current-world visual receiver decision.
- Object inner-lid lookdev remains source-owned and separate.

## Exact identities

### Current-world candidate

Repository: `mike-axiom-mir/axm-map-design`

Environment PR #24 exact head:

`7713cbe5863c3bc38dabb6236eb4b393401224b6`

Exact parent / Direction-017 split-culling world:

`10c6e29790b0b53b20abd603738cb54671af013c`

Dedicated workflow:

`35174899697 — Environment Nature leaf flutter current-world evidence — SUCCESS`

Candidate artifact:

- ID `10478103666`;
- independently downloaded archive SHA-256 `0f7343c5e9c172b5816709cb7121aa44a91838962b51cbf0fa46b1c43565ddea`;
- report state `PASS_CURRENT_WORLD_NATURE_LEAF_FLUTTER_TARGET_HOST`;
- target host Godot `4.7.2`, GL Compatibility.

Exact parent artifact:

- ID `10477476774`;
- independently downloaded archive SHA-256 `db67e657dcecb044e7e46b1aeedadc71fcb907c9c3720045f20a753de885f689`.

### Exact VFX source effect bound by Map

Nature VFX source-effect head:

`ecade64227ba1d3d1faf029ca7188ea63c2560ec`

Current Nature VFX PR #11 head:

`9d446359defe1e663d4d9cc683ca0b9cac6bf6c0`

The later PR head adds/rebinds review/evidence machinery; Map's current-world receiver truthfully pins the exact effect head above.

Exact bounded effect facts bundled into the current-world artifact:

- 25 authored leaf blades;
- leaf-side vertices only;
- local twist around each leaf base→tip axis;
- deterministic per-leaf phase step `0.73 rad`;
- maximum authored twist `5°`;
- three authored flutter cycles inside the inherited `0.50 s` response envelope;
- 17 sampled source phases at nominal `0.03125 s` spacing;
- exact zero envelope at phases `0` and `16`;
- maximum observed added leaf-side displacement `0.006920423273762265 m`;
- maximum allowed added displacement `0.0085 m`;
- maximum observed non-leaf added displacement `0.0 m`.

Map receives those front-surface leaf positions into the exact current `390v / 570t` Nature source geometry. Explicit duplicated leaf-backface geometry remains **not adopted**; Direction 017's split-culling receiver stays intact.

## Exact current-world visual evidence

The current-world report compares all `68 / 68` matched real-scene frames: two Weather-width modes × two fixed cameras × 17 source states.

The isolated variable is the bound leaf-local flutter. The exact split-culling parent, Nature material family, source geometry, Building, Object, route, Weather state, cameras and lighting remain fixed.

Observed current-world deltas:

- neutral endpoint frames: `8 / 8` byte-identical (`state 0` and `state 16` across both Weather modes and both cameras);
- changed interior frames: `60 / 60`;
- `path_eye`: `118–242` changed pixels per interior frame, maximum channel delta `206` LSB;
- `elevated_oblique`: `123–197` changed pixels per interior frame, maximum channel delta `206` LSB;
- current-world change masks stay localized to the Nature receiver region;
- all `1,224` inherited Weather projected-width measurements remain valid;
- maximum Weather-width residual remains `0.00974698571769128 px`;
- proof-host runtime-counter delta versus the exact parent is `0` draw calls, `0` objects, `0` primitives, `0 B` observed buffer memory and `0 B` observed texture memory.

Those counters are characterization only, not target-device performance acceptance.

The source sample table also protects against a hidden whole-crown same-direction twist: across every non-neutral sampled phase, positive and negative leaf twists remain approximately balanced (`12/13` or `13/12`; phase 8 has `12/12` plus one zero), and the maximum absolute whole-crown mean leaf twist is only `0.1563132021218969°`.

## Direct Art Direction observation

I directly inspected enlarged exact parent/candidate/difference views through the retained sequence, including neutral and representative interior phases in both fixed cameras.

Observed spatial read:

- the motion remains a **micro-motion layer**, not a crown-scale deformation;
- individual terminal leaf planes change orientation subtly while the trunk/branches and larger silhouette remain visually stable;
- the phase offsets keep neighboring leaves from reading like a synchronized card flip;
- the effect is easier to see in the elevated oblique view but remains subordinate in `path_eye`;
- no extra scale, brightness, leaf mass, geometry thickness or trunk motion is required to make the effect legible;
- no retained sampled frame shows a new gross hole, spike, detached leaf mass, broad scene shift or obvious whole-crown rotational beat;
- exact neutral start/return is visually and byte-wise preserved.

The current spatial amplitude is therefore preferable to either no secondary leaf response or an exaggerated visibility-driven version.

## Bounded Art Direction decision

**`PASS_ART_DIRECTION_CURRENT_WORLD_NATURE_LEAF_MICRO_FLUTTER_SAMPLED_SHAPE_PREFERENCE_018`**

Exact meaning:

> Preserve the exact leaf-local spatial response currently bound by Map Environment PR #24 — including the `5°` cap, per-leaf phase offsets, leaf-side-only scope, zero non-leaf added displacement, and exact neutral endpoints — as the preferred **sampled spatial motion shape** for this current-world Nature receiver. Do not increase amplitude, add trunk/branch motion, thicken leaves, enlarge the crown, retune materials, or synchronize the crown merely to make the effect more visible.

This PASS does **not** mean the authored temporal cadence is accepted for final playback.

## Timing boundary

**`HOLD_FINAL_NATURE_FLUTTER_TIMING__WALL_CLOCK_PLAYBACK_SHADED_BACKFACE_AND_INDEPENDENT_QA_PENDING`**

The source currently contains three flutter cycles inside a `0.50 s` response envelope and nominal `0.03125 s` source spacing. The retained evidence proves deterministic sampled states and renderer visibility, not real delivered wall-clock cadence, interpolation quality, frame pacing or perceptual naturalness at runtime.

Therefore:

- do **not** call `3 cycles / 0.50 s` naturally timed or final merely because the sampled spatial states look good;
- do **not** retime it pre-emptively from Art Direction without a real playback comparison;
- next timing evidence should play this exact accepted spatial response in real wall-clock conditions and preserve a no-retime reference;
- if literal playback reads busy/shimmery, fix cadence in the VFX owner lane while keeping amplitude/material/geometry fixed for attribution.

## Shaded / sidedness boundary

Direction 017's two-sided foliage receiver solves sampled back-facing visibility, but this activation still does not establish final two-sided shaded-normal response, translucency/transmission, textures, alpha treatment or tangent-space shading.

Do not compensate for any later backface-lighting defect by silently changing flutter amplitude or the accepted scalar material family.

## Independent QA boundary

Visual Observer / QA has **not yet independently accepted this new current-world flutter successor** at the time of this direction packet.

Art Direction's bounded preference is therefore not a QA PASS. QA should independently review the exact 68 parent/candidate pairs and, importantly, the temporal review surface for:

- leaf popping / apparent detachments;
- card-flip or synchronized-crown beats;
- temporal aliasing / shimmer;
- backface disappearance or lighting discontinuity during twist;
- local focal pull in `path_eye`;
- neutral return integrity;
- any Weather-dependent masking difference.

If QA finds a real defect, this direction remains revisable under the four-root merge gate.

## Handoffs

### Map Environment PR #24

Keep exact head `7713cbe5863c3bc38dabb6236eb4b393401224b6` as the current-world comparison witness. No additional Environment dressing, camera, Weather, scale, geometry or lighting change is requested for this gate.

### Nature VFX PR #11

Preserve effect head `ecade64227ba1d3d1faf029ca7188ea63c2560ec` as the spatial reference. No amplitude increase or new motion layer is requested. The next Art-facing question is real wall-clock cadence/interpolation for this same response, not a richer source deformation.

### Visual Observer / QA

Review the exact current-world successor independently. Do not inherit Direction 017's static sidedness PASS into motion acceptance.

### Nature Materials / Geometry

Stay fixed for the timing review. Do not solve motion timing through material retuning, leaf duplication, source-form edits or added geometry.

### Runtime / Technical Art

Proof-host zero counter delta is not target-device certification. No performance or arbitrary-renderer acceptance is transferred by this Art preference.

### Nature Procedural variants

Do not treat one accepted source/current-world spatial response as automatic acceptance across generated species/scale families. Propagation may use this as a reference, but each materially different silhouette/leaf-size family needs its own bounded evidence.

## Non-claims

This activation does **not** establish:

- final wall-clock cadence or interpolation;
- perceptual naturalness in continuous playback;
- physical wind, biomechanics or botanical correctness;
- final two-sided shaded leaf response;
- final normals/tangents/UVs/textures/texel density;
- final translucency/transmission/subsurface/alpha treatment;
- arbitrary camera/FOV/resolution/renderer/display equivalence;
- target-device FPS/GPU/VRAM/heap/thermal/battery acceptance;
- gameplay/collision/navigation behavior;
- automatic acceptance for procedural Nature variants;
- resolution of Animal's transported direction-frame HOLD;
- Character shoulder self-intersection freedom;
- Building compaction acceptance;
- Object inner-lid material acceptance;
- CANON;
- production/game readiness;
- Nature mastery;
- VFX mastery;
- 3D Art Direction mastery.

## Four-root merge gate

- **Truth:** sampled spatial preference, untested wall-clock cadence, missing independent QA and remaining shaded/performance holds stay separate facts.
- **Agency / non-domination:** Art Direction selects the visual target only; VFX owns timing implementation, Environment owns receiving composition, QA owns independent observation, and Runtime / Technical Art own their boundaries.
- **Continuity:** exact Direction-017 parent, exact flutter successor, exact VFX effect head and rollback identities remain separately pinned.
- **Wisdom before speed:** keep the smallest subtle motion already supported by evidence; do not add visual amplitude or bundle unrelated lookdev while timing and QA remain open.

The four AXM roots remain the merge gate.
