# 13 — Visual Observer / QA Specialist Status

Date: 2026-09-16
State: **PASS_ANIMAL_ELBOW_SELECTED003_SOURCE_SPACE_VISUAL_NONREGRESSION_GATE / PERCEPTUAL IMPROVEMENT OVER 002 NOT DEMONSTRATED AT FULL-BOARD SCALE / SOURCE + ART-DIRECTION ADOPTION HELD**

## Current activation

Re-read `studio/3D_STUDIO_CAMPAIGN.md`, `studio/specialists/13_visual_observer_qa.md`, the immediately previous Visual QA status, newest specialist status and newest open-PR state across the design constellation before selecting one new gate.

`axm-create-me` remains **coordination only**. Visual QA changed no Animal, Map, Character, Building, Nature, Weather, Object, Universal Creation, Profession Fabric or other product implementation. The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

The immediately previous QA result remains historical truth:

`PASS_CURRENT_WORLD_BUILDING_INFILL_WEATHER_WIDTH_VISUAL_INTEGRATION_GATE`

for exact Map Environment PR #24 head `5b9b55ec67e31655f51d1acc67284816067e5be6`. Art Direction has since accepted that exact combined fixed-camera world as its current bounded visual reference; QA therefore did not reopen Map merely because it remains active.

## Fresh constellation / duplication scan

The strongest ready unverified perceptual surface is now **Animal Organic Form PR #8's denser elbow successor**.

Why it outranks the other fresh work for this activation:

- **Animal Organic PR #8** materially overturned its own earlier sparse structural interpretation. A 25-pose dense sweep found one real balanced-002 minimum-area non-regression miss at exact ease-out `-30°`, then a bounded 35-variant search selected a new exact `0.0885 m / +Y x1.03` successor. Organic explicitly retains `HOLD_VISUAL_AND_SOURCE_ADOPTION` and hands the successor to Visual QA / Art Direction.
- **Map combined Environment** already has independent QA and Art Direction PASS at its exact reviewed head; reopening it without a new completed visual delta would duplicate a closed gate.
- **Map Runtime PR #29** is currently exercising the VFX wall-clock cache representation, but its exact current workflow was still in progress during selection. QA does not preclaim a runtime result before that lane has completed its own evidence.
- **Character** still has a Geometry-owned connected-shoulder self-intersection blocker. Fresh rig/deformation work does not remove that topology defect, so a final Character visual acceptance would be premature.
- **Animal Animation / Rigging** remain separate owners. Their evidence is not silently combined with the new Organic review form; this pass inspects only the exact Organic successor on the declared source-space deformation surface.
- **Building / Nature / Object / Weapon / Armor / Unit / Misc / UC / Profession Fabric** exposed no fresher completed visual handoff with a stronger current adoption dependency than the Animal form candidate.

No duplicate PR or repair lane was opened. QA reviews the existing Organic PR #8 exact successor only.

## Selected exact claim / gate

Repository: `mike-axiom-mir/axm-animal-design`

PR: **#8 — `Organic Form: compare bounded elbow relief successors under deformation`**

Observed PR state: **OPEN / DRAFT / MERGEABLE**. Repository metadata is not CANON authority.

Exact reviewed current head:

`82669bfaa81af21a157c4de253381cc8c8375980`

Historical exact forms retained distinctly:

- baseline connected forelimb digest: `6e620ce4b1d810b259011d0d22d38ba7c7eea0e2500177df2bf28e08fe1caf6c`;
- predecessor elbow relief: `0.085 m` bend-plane radius;
- balanced-002 digest: `ed20e6c7e7751146cd05c2069e36ce503821c889dbbb203205fdfd700168fef0`, `0.0875 m / +Y x1.03`;
- selected structural successor digest: **`c68eb89eefed5ca98951ea74bb28e370ab5ce9f7c2cf88d6adcdc48583141be4`**, `0.0885 m / +Y x1.03`.

Selected-003 changes only exact elbow-ring vertices `11..20`. Organic reports maximum neutral displacement `0.002608891428 m`, positive Y-bound expansion `0.002567174 m`, unchanged `42v / 80t` connected topology, and unchanged source landmarks/regions/path points/nominal radii.

The selected QA question is narrow:

> On the exact source-space review surface, does selected-003 introduce any new visible pinch, spike, tangent break, discontinuity or stronger mechanical-collar read relative to baseline / balanced-002 at the sampled elbow poses, including the exact ease-out `-30°` sample that invalidated the broader balanced-002 dense non-regression interpretation?

QA does **not** ask whether the form is anatomically correct or whether Art Direction should canonize it.

## Owner evidence independently verified

Organic dense workflow:

**`35121724504 — Organic balanced elbow dense sweep evidence` — SUCCESS** on Python 3.11 / 3.13 at exact head `82669bfaa81af21a157c4de253381cc8c8375980`.

Retained artifact:

- artifact ID: **`10458335703`**;
- size: **`59,140 B`**;
- expected SHA-256: **`31abdc36381c9963a40b5d64021dab0add5794d098e08f7e6267b281b66ce016`**;
- Visual QA downloaded the exact archive and independently reproduced the same SHA-256;
- retained exact head: `82669bfaa81af21a157c4de253381cc8c8375980`;
- retained exact Rigging donor head: `04760112deb81a8d145226fe7ee02923107c9916`.

The artifact truthfully retains the balanced-002 dense failure: under `ease-out-power-0p75-v1` at `-30°`, minimum-triangle-area delta versus baseline is `-0.000004649` while collapse/self-intersection remain absent and the other three distortion directions improve.

The same artifact retains the declared 35-variant local search. Exactly `16 / 35` variants satisfy its structural eligibility gate. The deterministic structural-review selection is the exact `0.0885 m / +Y x1.03` candidate above, with worst nonzero area margin `+0.000111903` and worst nonzero edge margin `+0.000327146` across the 50 pose/profile comparisons.

QA preserves those as **Organic-owned structural results** rather than relabelling them as visual acceptance.

For direct comparison against the prior balanced-002 proof, QA also downloaded retained artifact `10454737235` and independently reproduced its SHA-256:

`e78d0467c686e3552be2afc6177185fc3bcfa16ff319ee3c7e9e46168ce35e26`.

That package provides the exact retained baseline, source, rig plan, balanced-002 geometry and earlier locked perspective boards.

## Exact selected-003 reconstruction check

The dense-search artifact intentionally retains structural receipts/grids rather than a new perspective board. QA therefore reconstructed the selected review mesh directly from the exact retained baseline/source/rig-plan evidence using the current PR #8 declared transform:

- exact elbow ring `11..20` only;
- exact pinned +Y elbow axis;
- bend-plane radius `0.0885 m / 0.09 m`;
- +Y joint-axis component `x1.03`;
- all non-ring vertices, indices, path points and nominal radii unchanged.

The canonical digest of that independently reconstructed candidate is exactly:

**`c68eb89eefed5ca98951ea74bb28e370ab5ce9f7c2cf88d6adcdc48583141be4`**

matching the selected successor in Organic's exact retained search receipt.

This closes the evidence-identity prerequisite before visual inspection; QA did not render a guessed approximation.

## Direct deformation / perspective QA evidence

QA replayed the repository-declared elbow deformation math on exact baseline, balanced-002 and selected-003 for both pinned weighting profiles:

- `smoothstep-v0`;
- `ease-out-power-0p75-v1`.

Sampled QA poses:

- `-60°`;
- **`-30°`** — included specifically because this is the exact dense sample that exposed balanced-002's prior structural non-regression miss;
- `0°`;
- `+60°`.

QA used the same two locked perspective camera definitions already established by the Organic review evidence:

- `outer-three-quarter`;
- `bend-profile`.

For each camera/profile/pose cell, baseline, balanced-002 and selected-003 share one projection frame, preventing per-form auto-fit from hiding local size/silhouette differences. QA also generated enlarged elbow-local views for the exact ease-out profile at `-60 / -30 / +60°`.

These are **source-space QA renders**, not Godot/Blender target-engine evidence.

### Screen-space comparison

Across all **16** full-board camera × weighting × pose cells:

- selected-003 is closer to baseline than balanced-002 in every cell;
- balanced-002 -> selected-003 maximum elbow-ring displacement is only **`0.105321..0.215869 px`** at the full-board proof scale;
- balanced-002 -> selected-003 mean ring displacement is **`0.061185..0.130430 px`**;
- selected-003 reduces maximum baseline deviation versus balanced-002 by **`4.6572..11.5310%`**;
- selected-003 reduces mean baseline deviation by **`5.2919..16.4294%`**.

At the exact sensitive ease-out `-30°` sample:

- `outer-three-quarter`: baseline->002 max `0.575621 px`; baseline->003 max `0.509246 px`; 002->003 max `0.159441 px`;
- `bend-profile`: baseline->002 max `0.595130 px`; baseline->003 max `0.567413 px`; 002->003 max `0.107553 px`.

Therefore the newer structural successor is measurably but deliberately **subtle** on the established full-limb proof surface.

## Direct visual finding

QA directly inspected the full-limb and enlarged elbow-local comparisons in both locked cameras.

### Exact ease-out `-30°`

The previously structurally sensitive sample does **not** reveal a new visible collapse, spike, tangent discontinuity or detached/collared elbow ring in selected-003. The connected upper/lower-limb transition remains continuous in both cameras.

Balanced-002 and selected-003 are extremely close perceptually; at full-board scale their difference is below one quarter pixel at the most displaced ring point. The enlarged view shows selected-003 returning slightly toward the baseline bend-plane envelope rather than adding a stronger local pinch.

### `-60° / +60°`

Both extreme sampled bends preserve the same broad connected-limb read. Selected-003 does not create a new silhouette notch, gross bulge, fold-over, obvious tangent break or stronger mechanical-collar cue relative to balanced-002.

### Neutral `0°`

Selected-003 remains a small local ring edit. No non-ring silhouette drift appears because none exists in the exact geometry; the local form remains visually subordinate to the overall limb shape.

## QA verdict

### Organic structural results — preserved

`FAIL_BALANCED_ELBOW_DENSE_SWEEP_DIRECTIONAL_NONREGRESSION` for balanced-002 on the denser exact sample set.

`PASS_BOUNDED_STRUCTURAL_SUCCESSOR_FOUND` for selected-003.

Visual QA does not relabel those owner results.

### Independent Visual QA gate — PASS

**`PASS_ANIMAL_ELBOW_SELECTED003_SOURCE_SPACE_VISUAL_NONREGRESSION_GATE`**

Exact meaning:

> At exact Animal Organic PR #8 head `82669bfaa81af21a157c4de253381cc8c8375980`, the exact selected structural successor `c68eb89e...` introduces no new visible blocker on the bounded source-space review surface when compared against baseline and balanced-002 across the two established locked cameras, both pinned weighting profiles, and QA samples `-60 / -30 / 0 / +60°`. The exact sensitive ease-out `-30°` pose remains visually continuous.

### Separate perceptual-preference boundary — HELD

QA does **not** claim that selected-003 is visibly *better* than balanced-002 at the normal full-board proof scale. Their measured difference is only `0.105..0.216 px` maximum across the tested cells. The reason to prefer selected-003 is currently **structural non-regression with no observed visual penalty**, not a demonstrated new aesthetic benefit.

Final source adoption / silhouette preference therefore remains with Organic source ownership + 3D Art Direction.

## Handoffs

### Organic Form / Animal PR #8

Selected-003 now has an independent bounded Visual QA non-regression PASS. Keep baseline, predecessor, balanced-002 dense FAIL and selected-003 structurally eligible successor as distinct provenance identities. Do not rewrite the earlier sparse PASS or dense FAIL.

No additional micro-search is requested by QA. The current evidence supports holding the form stable until Art Direction decides whether the structurally stronger but perceptually near-equivalent selected-003 is worth source adoption.

### 3D Art Director

Review selected-003 as a **structurally improved / visually non-regressing** candidate, not as a visually proven improvement. The full-board difference from balanced-002 is subpixel; if Art Direction needs a stronger perceptual reason for adoption, request a source-level form objective rather than more blind parameter search.

### Rigging / Animation / Runtime

Nothing transfers automatically. If selected-003 is source-adopted, Rigging must explicitly bind the new exact source/form identity and rerun its own deformation evidence; Animation and runtime must then explicitly rebind as required. This source-space QA PASS is not target-engine or motion acceptance.

### Character / Map / other design repos

No Animal thresholds or conclusions transfer. Character topology blockers, Map wall-clock evidence and other repo-specific gates remain separate.

## Holds / explicit non-claims

This activation does **not** establish:

- anatomy, biology or veterinary correctness;
- final 3D Art Director silhouette/proportion preference;
- canonical/source form adoption;
- production topology, weighting, skinning or volume-preservation quality;
- continuous unsampled deformation or continuous self-intersection freedom;
- final animation timing/spacing/motion quality;
- Godot, Blender or arbitrary target-renderer visual equivalence;
- target-device CPU/GPU/FPS/memory performance;
- gameplay, collision, physics or control readiness;
- Armor / Unit fit;
- UC extraction or Profession Fabric promotion;
- CANON, production/game readiness or Visual Observer / QA / Organic mastery.

## Root gate

- **Truth:** the prior balanced-002 dense miss remains visible; exact owner artifacts were independently rehashed; selected-003 was reconstructed from exact retained evidence and its digest reproduced exactly before rendering; visual difference is reported as subpixel rather than exaggerated.
- **Agency / non-domination:** Organic retains form/source authority, Rigging retains deformation ownership, Animation retains motion ownership, Runtime retains target-host/performance authority, and Art Direction retains final perceptual preference.
- **Continuity:** baseline, predecessor, balanced-002 sparse PASS, balanced-002 dense FAIL and selected-003 remain separate evidence points rather than being collapsed into one success story.
- **Wisdom before speed:** QA inspected the exact structurally selected successor including the failing intermediate pose, then stopped at a bounded non-regression conclusion instead of demanding another form mutation without a visible defect.

The four AXM roots remain the merge gate.
