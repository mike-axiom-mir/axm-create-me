# 13 — Visual Observer / QA Specialist Status

Date: 2026-09-17

State: **PASS_COMPACT_EAST_WALLCLOCK_SEQUENCE_EVIDENCE_IDENTITY_AND_SPATIAL_COHERENCE / FAIL_COMPACT_EAST_PROOF_HOST_RENDERED_TEMPORAL_PEAK_DELIVERY__CYCLE_B_PHASE08_OMITTED / BLOCKED_FINAL_COMPACT_EAST_MOTION_NATURALNESS__UNINSTRUMENTED_DISPLAY_TARGET_DEVICE_ART_PENDING**

## Current activation

Re-read `studio/3D_STUDIO_CAMPAIGN.md`, `studio/specialists/13_visual_observer_qa.md`, the preceding QA state, fresh owner-specialist status, and current render/runtime evidence across the constellation.

`axm-create-me` remains **coordination only**. QA changed no Map/Nature/VFX/Animation/Weather/Runtime product implementation, no source geometry or source timing, no camera/light/material, no Universal Creation product code, and no CANON state.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

## Selection / duplication scan

The strongest newly completed unverified visual/runtime claim is the new directly reviewable compact-east wall-clock rendered sequence on `mike-axiom-mir/axm-map-design` PR #44.

This is the exact evidence surface requested by the immediately preceding QA result and Art Direction: the previous head had valid exact keys and real-loop delivery characterization but no retained wall-clock raster sequence sufficient to inspect what the proof host actually rendered over time.

Fresh Object VFX PR #31 now has a green target-host mote receiver with independent Visual QA still open; Animal Runtime PR #29 has a fresh lifecycle optimization but explicitly no fresh shaded A/B. Those remain valid later QA surfaces, but neither displaces the direct continuity obligation on Map PR #44. QA therefore does not duplicate VFX, Runtime, Animation-authoring, or Art Direction ownership.

## Exact evidence identity

Repository: `mike-axiom-mir/axm-map-design`

PR: **#44 — `Animation: prove compact-east current-world exact-state playback`**

Exact head inspected: `84a186f087d8d7353cbfb98750f765d21bcc52be`.

PR state at inspection: **OPEN / DRAFT / UNMERGED / MERGEABLE**. Mergeability is metadata, not visual authority.

Exact receiving ancestry remains frozen:

- parent current-world VFX receiver: `29ef2d4cc4398b3f26290e4e1f1f10398ca9898c`;
- Nature VFX owner: `cef2ad78d8e36a55ada5dad07329f1a7125d48de`;
- exact-playback predecessor: `48dc93848aa336f9b6079ccfe738acbe539253ac`;
- duration `0.50 s`;
- source step `31.25 ms`;
- 16 unique loop states, phase `00..15`, with endpoint phase `16` retained only as duplicate-neutral witness;
- `NEAREST` interpolation / `DISCRETE` update / `LOOP_LINEAR`;
- Weather and west sapling frozen at phase `00`.

Current wall-clock evidence workflow `35259707312 — Animation compact-east current-world wall-clock sequence evidence` completed **SUCCESS**.

Retained artifact:

- ID `10514216406`;
- archive SHA-256 `f8753f79aafffc1506fb1c8384a19e3b34b7bf07c3432d05b3fcd310999ad0ef`;
- `23` retained `1100 x 720` PNGs;
- one full warmup loop before retention;
- two complete retained loops plus closing seam;
- monotonic timestamp, exact observed phase identity, cycle/wrap marker, and persistent receiver/player identity retained per frame.

QA independently downloaded the archive and reproduced the exact archive digest. All 23 retained PNG SHA-256 values match `report.json`.

## PASS — the requested wall-clock evidence surface is real and spatially coherent

**`PASS_COMPACT_EAST_WALLCLOCK_SEQUENCE_EVIDENCE_IDENTITY_AND_SPATIAL_COHERENCE`**

Independent inspection/recomputation confirms:

- every exact phase `00..15` appears at least once somewhere across the two retained cycles;
- repeated same-phase rasters are byte-identical across cycles;
- the retained neutral phase is stable at loop boundaries;
- no gross tree detachment, hole, broad silhouette corruption, unrelated world shift, Weather motion, west-sapling motion, or camera/light drift is reproduced in the 23 supplied frames;
- source/receiver identity remains frozen rather than being silently retimed or reshaped to satisfy the observer.

This closes the preceding QA request for a directly reviewable wall-clock rendered sequence. It does not by itself make that delivered motion visually acceptable.

## FAIL — one retained loop aliases away the visible motion peak

**`FAIL_COMPACT_EAST_PROOF_HOST_RENDERED_TEMPORAL_PEAK_DELIVERY__CYCLE_B_PHASE08_OMITTED`**

The new raster sequence makes the prior cadence limitation visually concrete rather than merely numerical.

Across the 22 retained frame-to-frame transitions:

- `12 / 22` advance one source phase;
- `10 / 22` advance two source phases.

The most consequential reproduced defect is in retained cycle B:

- actual consecutive rendered states: `P07 -> P09`;
- retained interval: **53.828 ms**;
- P07 and P09 are **byte-identical** in this symmetric authored response;
- actual raster delta between those consecutive retained frames: **0 pixels**;
- exact P08 is the visible peak state and is omitted from that cycle;
- the retained exact P08 witness differs from P07/P09 on **841 pixels >1 LSB**.

Therefore cycle B contains an actual identical-frame hold across the peak while the distinct peak raster is absent. That is a direct temporal-alias result in this instrumented proof-host rendered sequence.

A second bounded witness appears at the closing seam of cycle B:

- actual retained transition: `P14 -> P00`;
- interval: **53.824 ms**;
- P15 is omitted in that cycle;
- actual P14 -> P00 delta: **5,232 pixels >1 LSB**;
- adjacent exact P15 -> P00 seam witness: **3,866 pixels >1 LSB**.

The neutral endpoint itself is geometrically correct. The larger second seam step is delivery/capture aliasing, not a source-geometry seam failure.

## BLOCKED — final motion naturalness and target presentation

**`BLOCKED_FINAL_COMPACT_EAST_MOTION_NATURALNESS__UNINSTRUMENTED_DISPLAY_TARGET_DEVICE_ART_PENDING`**

The current evidence is explicitly `RenderingServer.frame_post_draw` plus image-readback instrumentation. It proves what this proof receiver retained, but not uninstrumented display scanout or target-device presentation.

Accordingly, the scoped FAIL above does **not** establish:

- that target displays will necessarily show the same identical-frame hold;
- target-device stutter or frame loss;
- a Nature/VFX source-motion defect;
- a requirement to retime, smooth, amplify, or rewrite the frozen source;
- final Art Direction timing/rhythm/naturalness preference.

Art Direction retains perceptual preference. Runtime retains target-device delivery/performance authority. Animation retains playback/source binding. Nature/VFX retain source response. QA requests no product repair from this result.

## Handoff / review return

Independent QA was returned to Map PR #44 exact head `84a186f087d8d7353cbfb98750f765d21bcc52be` as review **`5240143353`**.

The most useful next evidence, if final presentation is to be promoted, is an uninstrumented or target-device presentation path that can distinguish proof-harness capture alias from viewer-visible delivery. Source retiming is not authorized by QA.

## Prior retained QA state

The immediately preceding PR #44 result remains historical truth and is not rewritten:

- `PASS_COMPACT_EAST_ANIMATIONPLAYER_EXACT_KEY_BINDING_AND_REPRESENTATIVE_VISUAL_RESPONSE`;
- `FAIL_COMPACT_EAST_PROOF_HOST_PROCESS_FRAME_OBSERVATION_OF_FULL_31_25MS_SOURCE_SLOT_CADENCE`;
- `BLOCKED_FINAL_COMPACT_EAST_CONTINUOUS_MOTION_VISUAL_ACCEPTANCE__REVIEWABLE_WALL_CLOCK_SEQUENCE_DISPLAY_RUNTIME_ART_PENDING`;
- review `5239494610` on exact predecessor head `48dc93848aa336f9b6079ccfe738acbe539253ac`.

The new wall-clock packet resolves only the prior **missing reviewable raster-sequence** blocker. It strengthens, rather than erases, the old delivery-cadence boundary by showing a concrete peak-alias case.

Earlier Map sampled-state and Animal direction-frame QA results also remain historical evidence in Git history; no acceptance is transferred between systems.

## Explicit non-claims

This activation does **not** establish physical wind or biomechanics; smooth interpolation; full 31.25 ms source-slot presentation delivery; uninstrumented display scanout; target-device CPU/GPU/FPS/VRAM/thermal behavior; arbitrary-camera readability; final leaf-sidedness/material acceptance; Runtime controller/state-machine correctness; gameplay/collision/navigation acceptance; final Art Direction timing/naturalness preference; CANON; game readiness; production readiness; or Visual Observer / QA mastery.

## Four-root gate

- **Truth:** exact artifact and all retained PNG identities are independently rechecked; the new sequence is passed as evidence while its concrete peak alias is failed in the same report.
- **Agency / non-domination:** QA does not retime Nature/VFX, change Animation semantics, rewrite Runtime policy, or convert a proof-host artifact into Art/CANON authority.
- **Continuity:** the prior exact-key/process-frame PASS/FAIL/BLOCKED state remains explicitly preserved; the new head is a successor evidence surface rather than a silent rewrite.
- **Wisdom before speed:** the directly observed alias is localized to the instrumented proof-host sequence, and no source repair is requested before display/target evidence establishes that the viewer actually receives the same defect.

The four AXM roots remain the merge gate.
