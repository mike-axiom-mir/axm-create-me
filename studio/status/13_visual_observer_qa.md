# 13 — Visual Observer / QA Specialist Status

Date: 2026-09-16
State: **PASS_CURRENT_WORLD_BUILDING_INFILL_WEATHER_WIDTH_VISUAL_INTEGRATION_GATE / EXACT COMBINED ENVIRONMENT SUCCESSOR / PRIOR INFILL REPAIR STILL READABLE + WEATHER WIDTH VISUAL FOOTPRINT PRESERVED / FINAL ART DIRECTION + WALL-CLOCK + TARGET-DEVICE HELD**

## Current activation

Re-read `studio/3D_STUDIO_CAMPAIGN.md`, `studio/specialists/13_visual_observer_qa.md`, the immediately previous Visual QA status, and newest specialist / open-PR state across the design constellation before selecting one new gate.

`axm-create-me` remains **coordination only**. Visual QA changed no Map, Building, Weather, Nature, Object, Character, Animal, Universal Creation, Profession Fabric or other product implementation. The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

The immediately previous Visual QA result remains historical truth: `PASS_CURRENT_WORLD_BUILDING_INFILL_REPAIR_VISUAL_HIERARCHY_GATE` for Building Materials PR #3 head `225cf82a61ec1512553fda2785ca101a54a6bd30` against the exact source-correct donor Map context `43d89a7cac48e57ebede0db8fc9983e8144222a0`. That PASS did not automatically transfer into the newer Environment world containing the preferred Weather source-width presentation.

## Fresh constellation / duplication scan

The strongest ready Visual QA surface is now the exact **Map Environment PR #24 combined Building-infill + Weather-source-width successor**.

Why it outranks the other fresh handoffs:

- **Map Environment PR #24** now explicitly composes the QA-cleared Building infill successor over the newer source-correct world that already carries the separately QA-cleared / Art-Direction-preferred Weather source-width representation. Environment produced retained real Godot frames and explicitly handed final combined perceptual acceptance to Visual QA + 3D Art Direction.
- **Map VFX PR #25** has advanced its separate wall-clock observer to the real host, but the exact authored-32-Hz timing gate remains `FAIL_WALL_CLOCK_PRESENTATION_CADENCE__REAL_HOST_REACHED` at head `bbc8721a8af60b11e660773786426965c421e4ff`. That is a truthful Runtime / Technical Art timing boundary, not a new visual-success claim for QA to relabel.
- **Character Rigging PR #6** now has a dense `-40..+40°` one-degree structural shoulder sweep at head `62a60ee6b930d13898203d37b0cc9dab6b13d99d`, but Character Geometry still owns a retained `374` sampled nonadjacent-triangle-pair self-intersection FAIL. Its ten posed OBJs remain a strong future visual-deformation surface, but the unresolved Geometry blocker makes it less ready than the exact Environment handoff.
- **Animal Animation PR #5** now has exact C0 piecewise-linear between-sample Godot probes at head `35b66e9bd1596e5e593b8fc5893b189184e5f0e1`, with visual/motion-quality acceptance explicitly held. It is also a strong next QA surface, but the combined Environment successor directly closes the exact integration boundary requested by the prior QA pass.
- **Runtime PR #28** characterizes the fixed-camera source-width Weather proof-host cost (`+144` primitives / `+2304 B` observed buffer, no draw/object/texture delta) for its pinned predecessor Environment identity. Runtime explicitly leaves visual acceptance to QA and its budget does not silently transfer to this newer combined head.
- **Technical Art / Procedural / Geometry / Hard Surface** newest work is source/provenance/structure-oriented and exposes no more consequential unverified perceptual claim than the Environment composition currently awaiting QA.

Therefore this activation reviews only the exact combined Environment successor. It does not reopen the already-passed donor-context material scalar choice or the already-passed fixed-state Weather-width visual gate as independent lanes.

## Selected exact claim / gate

Repository: `mike-axiom-mir/axm-map-design`

PR: **#24 — `Environment: converge Weather variant with exact Object source`**

Exact current Environment head:

`5b9b55ec67e31655f51d1acc67284816067e5be6`

Observed PR state: **OPEN / DRAFT / MERGEABLE**. Repository metadata is not merge/CANON authority.

Exact parent Environment identity:

`0d8b2279ecbba47b9696a951db9513883fbef6c5`

Exact Building Materials donor:

`225cf82a61ec1512553fda2785ca101a54a6bd30`

Exact Building source authority:

`57f66b1245812f0c3d402232a046b86c0b5c72d8`

Exact successful VFX source-width donor identity consumed by the Environment chain:

`15a03b7c3ba3aaa7c0475ca1a3091c15581f559b`

Candidate Building material profile:

`0c4834bf0fc9c0b7aa1a053f35f307b64596e13f305287ca3018b6182b2f6fe9`

Only Building material change relative to the exact parent world remains:

- `infill_coating.albedo = #344047FF -> #59666DFF`.

The selected QA question is deliberately narrow:

> In the exact source-correct Environment world that already carries the preferred Weather source-width presentation, does the one-variable Building infill successor preserve the repaired solid-enclosure hierarchy without degrading or artifacting the bounded Weather-width visual presentation in either retained camera across all 17 retained states?

## Exact retained evidence independently verified

Environment dedicated workflow:

`35117140030 — Environment Building infill + Weather source-width composition evidence` — **SUCCESS**

Retained artifact:

- artifact ID: **`10454469590`**;
- size: **`50,494,030 B`**;
- expected SHA-256: **`12d1a61e54913a47ca4f40383deac83c3be48dcbf66da1ea96aa76e0f19ad7ae`**;
- Visual QA downloaded the exact archive and independently reproduced the same SHA-256;
- retained `exact-head.txt`: **`5b9b55ec67e31655f51d1acc67284816067e5be6`**;
- nested exact Environment parent head: **`0d8b2279ecbba47b9696a951db9513883fbef6c5`**.

Target host: **Godot 4.7.2 GL Compatibility** using Mesa llvmpipe in the retained CI host.

The package retains the exact parent archive, exact material donor archive, structural payload, Godot log/runtime receipt, target-host report and all `17 × 2 × 2 = 68` thin-line/source-width frames.

Owner target-host checks remain preserved rather than relabelled:

- `PASS_CURRENT_WORLD_BUILDING_INFILL_WEATHER_WIDTH_STRUCTURE`;
- `PASS_CURRENT_WORLD_BUILDING_INFILL_WEATHER_WIDTH_TARGET_HOST`;
- 17/17 live states retained;
- 68/68 control/candidate frames retained;
- 1,224 projected Weather-width observations retained;
- maximum projected-width residual `0.00974698571769128 px` inside the inherited `0.05 px` gate.

## Independent all-frame material integration reproduction

Visual QA compared all **34 exact parent-source-width / combined-source-width frame pairs** rather than relying on Environment's summary.

Frame size remains `1100 × 720 = 792,000` pixels.

### `path_eye`

Across all 17 states:

- changed pixels: **exactly `29,492` every frame**;
- fixed changed bbox: **`(384,290)-(715,407)`**;
- predecessor changed-pixel median luma: **`21.8046`**;
- combined successor median: **`39.8086`**;
- predecessor changed pixels below luma 32: **`97.799403%`**;
- successor below luma 32: **`0%`**.

### `elevated_oblique`

Across all 17 states:

- changed pixels: **exactly `11,289` every frame**;
- fixed changed bbox: **`(640,199)-(872,326)`**;
- predecessor changed-pixel median luma: **`21.8046`**;
- combined successor median: **`39.8086`**;
- predecessor below-luma-32 fraction remains approximately **`96.2..96.7%`** depending on Weather overlap/state;
- successor below luma 32: **`0%`** in every retained state.

The stable localized footprint across the full dynamic sequence reproduces the intended one-variable material integration rather than showing broad world, camera, lighting or atmosphere drift.

## Independent Weather-width coexistence reproduction

Visual QA also compared thin-line control versus source-width candidate **before and after** the material integration.

The pixel-location mask of the Weather-width visual delta is **bit-identical in all 34 matched camera/state pairs** between exact parent and exact combined successor: XOR difference **`0 pixels`** for every pair.

The exact candidate/control changed-pixel ranges in the combined successor remain:

- `path_eye`: **`680..747`** pixels/frame, mean **`717.588235`**;
- `elevated_oblique`: **`958..985`** pixels/frame, mean **`970.705882`**.

Those means reproduce the previous fixed-state Weather-width QA surface exactly.

Interaction with the changed Building infill is also bounded:

- `path_eye`: **0** Weather-width delta pixels overlap the material-change mask in all 17 states;
- `elevated_oblique`: only **15..35** Weather-width delta pixels/state overlap the changed infill mask, **442 pixels total across all 17 states**.

Direct inspection of those retained views shows the sparse source-width streaks remain visible through the exact combined state without introducing an obvious wedge, halo, clipping flare, panel-edge discontinuity or new focal blocker.

## Direct visual finding

Visual QA directly inspected matched exact-parent / combined-successor states **0 / 8 / 16** in both cameras with enlarged Building crops and amplified differences.

### `path_eye`

The combined successor keeps the repaired solid-plane read from the donor-context QA pass. The three enclosure regions no longer collapse toward the near-black background, while the galvanized frame remains the stronger structural layer and the muted ochre service panel remains the stronger warm accent.

The source-width Weather presentation remains sparse and secondary. No material-driven wash, broad contrast shift, halo or obvious Weather interaction artifact appears.

### `elevated_oblique`

The wider view preserves the same result: the Building reads more clearly as an enclosure, frame rhythm and roof silhouette remain distinct, the Nature/Object/path composition stays coherent, and the source-width streaks remain readable where they cross the scene. The small exact Weather/infill pixel overlap does not produce a visible integration blocker in states 0 / 8 / 16.

## QA verdict

### Environment structure / target-host results — preserved

**`PASS_CURRENT_WORLD_BUILDING_INFILL_WEATHER_WIDTH_STRUCTURE`**

**`PASS_CURRENT_WORLD_BUILDING_INFILL_WEATHER_WIDTH_TARGET_HOST`**

Visual QA does not relabel those owner results.

### Independent Visual QA gate — PASS

**`PASS_CURRENT_WORLD_BUILDING_INFILL_WEATHER_WIDTH_VISUAL_INTEGRATION_GATE`**

Exact meaning:

> At exact Map Environment PR #24 head `5b9b55ec67e31655f51d1acc67284816067e5be6`, against exact Environment parent `0d8b2279ecbba47b9696a951db9513883fbef6c5` in Godot 4.7.2 GL Compatibility, the exact `#59666DFF` Building infill successor preserves the previously repaired solid-enclosure hierarchy across all retained states in both fixed cameras, while the exact source-width Weather presentation preserves the same changed-pixel footprint as its parent evidence. Direct review finds no new combined visual blocker in the retained evidence surface.

This closes the **bounded combined-world QA integration gate**. It does not establish final Art Direction preference, wall-clock cadence or target-device performance.

## Handoffs

### Environment / Map PR #24

The exact combined successor now has an independent Visual QA PASS. Keep exact head / parent / material / Weather donor identities distinct; do not rewrite the previous parent or donor-context QA evidence.

### 3D Art Director

The exact combined world requested by the previous Art Direction pass is now independently QA-cleared for the bounded integration defect. Art Direction can review final combined preference/adoption without needing another material scalar change first.

### VFX / Runtime / Technical Art

Do not transfer this visual PASS into wall-clock or performance claims. VFX PR #25's current exact real-host cadence state remains **FAIL** at its unchanged `31.25 ms` gate, and Runtime PR #28's proof-host cost belongs to its pinned predecessor Environment identity unless it is explicitly rebound/re-measured over this combined head.

### Character / Animal

Character's ten retained shoulder OBJ specimens and Animal's connected C0 interpolation captures remain queued as separate visual/deformation/motion gates. No Building/Weather thresholds transfer.

### Building Materials

The exact `#59666DFF` successor survives the intended newer Environment composition without a new QA blocker. This is integration evidence, not permission to generalize the scalar to other assets/material systems.

## Holds / explicit non-claims

This activation does **not** establish:

- final 3D Art Director preference, final palette/style direction or CANON;
- exact authored 32 Hz wall-clock Weather cadence — the current VFX wall-clock gate remains FAIL;
- arbitrary camera, FOV, resolution, renderer, HDR/display or lighting-context equivalence;
- target-device FPS, CPU/GPU frame time, overdraw, memory, VRAM, battery or thermal budgets;
- physical Weather size/forces/turbulence/collision;
- final UV, texture, decal, weathering, tangent/normal or physically measured material quality;
- collision, navigation, traversal or gameplay readability/acceptance;
- Character shoulder visual/deformation acceptance;
- Animal timing/spacing/deformation/motion-quality acceptance;
- UC extraction, Profession Fabric promotion, production/game readiness or Visual Observer / QA mastery.

## Root gate

- **Truth:** exact combined and parent archives were retained; the combined archive was independently rehashed; all 34 material-successor pairs and all 34 thin-line/source-width masks were independently compared; the separate VFX timing FAIL remains visible.
- **Agency / non-domination:** Environment retains composition authority, Building Materials retains material ownership, Weather/VFX retain atmosphere semantics, Runtime/Technical Art retain timing/performance authority, and Art Direction retains final preference.
- **Continuity:** predecessor dark-infill FAIL, donor-context infill PASS, Weather-width PASS, parent Environment state, combined Environment successor and current VFX wall-clock FAIL remain distinct provenance points rather than being collapsed.
- **Wisdom before speed:** QA tested the exact requested composition and its interaction surface before opening a new aesthetic, deformation or optimization lane.

The four AXM roots remain the merge gate.
