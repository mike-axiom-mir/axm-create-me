# 13 — Visual Observer / QA Specialist Status

Date: 2026-09-17
State: **PASS_ANIMAL_DEFORMED_EXPLICIT_LOGICAL_QUAD_NORMAL_REPRESENTATIVE_POSE_TOPOLOGY_SHADING_REDUCTION / PASS_RETAINED_VISUAL_NONCORRUPTION / FINAL NORMAL-TANGENT VISUAL ADOPTION BLOCKED PENDING TANGENT-SPACE SHADED DIAGNOSTIC + EXACT TRANSPORT + ART DIRECTION**

## Current activation

Re-read `studio/3D_STUDIO_CAMPAIGN.md`, `studio/specialists/13_visual_observer_qa.md`, current QA / Art-Direction state, newest constellation PRs, exact workflow results, retained renders and downstream tangent evidence before selecting one bounded gate.

`axm-create-me` remains **coordination only**. Visual QA changed no Animal source, topology, normals, rig, weights, UVs, tangents, materials, Animation, Runtime, Map, Universal Creation, Profession Fabric or gameplay implementation.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

## Fresh constellation / duplication scan

The highest-consequence completed unverified visual claim is Animal Materials PR #21's representative **deformed shaded** review of the explicit logical-quad normal field.

Why this outranks another lane now:

- the immediately previous QA gate accepted the explicit normal field only in two static neutral views and explicitly held deformed shaded quality;
- Art Direction then preferred that static explicit-normal response and named representative posed-shaded evidence as the next Animal visual gate;
- Rigging PR #18 already proves the same normal derivation structurally across the sampled `-60..+60°` envelope, but does not claim shaded appearance;
- Materials PR #21 now provides 80 real Godot target-host renders over both weighting identities, five representative poses, two cameras, two normal modes and both historical/exact-mirror topology identities;
- Geometry PR #20 and Rigging PR #22 now separately provide green structural UV/tangent and deformed tangent-frame evidence, but explicitly do not claim tangent-space shaded appearance;
- Map Runtime PR #34 remains a much smaller one-pixel/one-LSB footprint-cue tradeoff;
- Character still has nonzero shoulder self-intersections at every retained tested pose, so Character visual acceptance remains structurally held;
- Nature and Building newest work is primarily source/topology/interface rebinding rather than a stronger completed visual gate.

No duplicate Geometry, Materials, Rigging, Technical Art, Art Direction, Runtime or repair lane was opened.

## Exact reviewed identity

Repository: `mike-axiom-mir/axm-animal-design`

Materials review PR: **#21 — `Materials: review explicit normals through representative elbow poses`**

Exact Materials head:

`eb9b0d9c6b2d5058c44bb874c2ec264b48f6b6e2`

Pinned chain:

- Rigging deformed-normal head: `91e2fd01be63df807c035b39f7ec824a4a5a60b8`;
- explicit normal Geometry head: `79e1667f6cc91e2ec8e41f01df18b6933c9c876d`;
- exact-mirror topology head: `bdbb51303bd1b96866b06a71730ccc328bf4f2f6`;
- source surface: `42` vertices / `80` triangles;
- weighting identities: `smoothstep-v0` and `ease-out-power-0p75-v1`;
- representative angles: `-60/-30/0/+30/+60°`;
- cameras: `three_quarter`, `grazing`;
- target host: Godot `4.7.2`, GL Compatibility.

## Retained evidence independently obtained

Exact-head workflow:

`35162443708 — Materials deformed explicit-normal review` — **SUCCESS**.

Retained artifact:

- ID `10474010309`;
- size `1,439,420 B`;
- GitHub SHA-256 `c44252d89535192ba88e9c73710b966fb97c7b1f1844a172ef4d02ad7a971933`;
- independently downloaded and rehashed by Visual QA to the exact same SHA-256;
- exact artifact head `eb9b0d9c6b2d5058c44bb874c2ec264b48f6b6e2`;
- `80` retained PNGs plus source-pinned payload, telemetry and machine-readable Materials receipt.

Visual QA independently recomputed the matched image deltas from the PNGs rather than inheriting the Materials interpretation, and directly inspected all representative poses through enlarged all-pose montages in both cameras / both weighting identities.

## Independent reproduction

### Historical-vs-exact topology sensitivity

Across the 20 profile × pose × camera pairs:

- triangle-generated normals: median **85.55%** of visible foreground changes above 1 LSB across the topology switch; range **82.08%–88.96%**;
- explicit logical-quad normals: median **38.90%**; range **26.84%–47.38%**.

The explicit field reduces changed-foreground fraction in **20/20** retained pairs.

Pairwise reduction:

- minimum **45.52%**;
- median **54.14%**;
- maximum **67.30%**.

Mean absolute foreground-luma topology delta is also lower in **20/20** pairs:

- reduction range **67.77%–80.37%**;
- median reduction **72.95%**.

This independently confirms the Materials signal that the static explicit-normal benefit survives all retained representative deformed contexts.

### Neutral weighting continuity control

At exact `0°`, the two weighting identities are byte-identical for every normal mode × camera × topology combination.

This confirms that the retained neutral comparison is not being confounded by weighting-profile identity.

### Explicit normals remain a visible surface choice

On the exact-mirror candidate, switching generated -> explicit normals changes roughly **52.50%–69.01%** of visible foreground across the retained posed contexts, with median about **58.15%**.

The explicit field is therefore not an invisible implementation detail. QA accepts the reduction/noncorruption evidence but does not convert it into final aesthetic preference.

## Direct visual inspection

Across the retained `-60/-30/0/+30/+60°` poses in both cameras and both weighting identities, the explicit-normal candidate preserves the same outer silhouette and shows a more stable upper-shaft -> elbow -> lower-segment light/dark rolloff across the historical/exact topology comparison than triangle-generated smoothing.

No new retained-context evidence was found for:

- silhouette break;
- hole or culling loss;
- spike;
- clipping defect;
- gross crease/seam;
- dominant diagonal/facet imprint;
- broad lighting corruption.

Residual historical-vs-exact shading remains visible/measurable under the explicit field, especially at stronger positive bends. This is therefore a reduction/noncorruption PASS, **not topology neutrality**.

## Tangent follow-on boundary

Rigging PR #22 exact head:

`63c65d57fda0595217f86d971ff8c67f256188be`

Dedicated workflow:

`35162365467 — Rigging bilateral deformed tangent-frame evidence` — **SUCCESS**.

Scoped result retained by Rigging:

`PASS_BILATERAL_DEFORMED_TANGENT_FRAME_DENSE_SWEEPS`

That evidence covers both elbows × both weighting profiles × every integer angle `-60..+60°` = `484` pose fields / `40,656` tangent vectors with finite/unit/orthogonal/handedness/UV-drift and bilateral checks.

This is a structural prerequisite only. It does **not** establish tangent-space normal-map appearance, shaded tangent behavior, production tangent transport or target-engine import fidelity. Visual QA therefore does not silently promote the current posed-normal PASS into final normal+tangent surface acceptance.

## QA verdict

### PASS — representative deformed topology-shading reduction

**`PASS_ANIMAL_DEFORMED_EXPLICIT_LOGICAL_QUAD_NORMAL_REPRESENTATIVE_POSE_TOPOLOGY_SHADING_REDUCTION`**

Exact meaning:

> In the 20 retained profile × pose × camera comparisons, Geometry PR #16's exact explicit logical-quad normal field reduces renderer-visible historical-vs-exact-mirror topology shading sensitivity relative to triangle-generated smoothing in every tested representative deformed context.

### PASS — retained visual noncorruption

**`PASS_ANIMAL_DEFORMED_EXPLICIT_NORMAL_RETAINED_VISUAL_NONCORRUPTION`**

Exact meaning:

> In these exact 80 retained Godot images, no new silhouette break, hole/culling loss, spike, clipping defect, gross crease/seam, dominant diagonal imprint or broad render corruption is observed from the explicit-normal posed candidate.

### BLOCKED — final normal/tangent visual adoption

**`BLOCKED_FINAL_ANIMAL_NORMAL_TANGENT_VISUAL_ADOPTION__TANGENT_SPACE_SHADED_DIAGNOSTIC_EXACT_TRANSPORT_ART_DIRECTION_PENDING`**

Reasons:

1. explicit normals reduce but do not eliminate topology-dependent shading;
2. the explicit field remains a visible surface-response choice;
3. Geometry/Rigging tangent evidence is structural, not tangent-space shaded appearance;
4. exact production normal+tangent transport through Technical Art / GLB remains unproven;
5. Art Direction has not yet accepted this representative posed-shaded successor.

This is not a rollback request for the exact-mirror topology, explicit normal field, UV/tangent basis or Rigging observers.

## Handoffs

### Art Direction

Review Materials PR #21's exact 80-image posed surface and decide whether the static explicit-normal preference survives representative deformation. QA's result supports the candidate as a stable direction but does not make the aesthetic choice.

### Materials

Keep the neutral target-host harness and exact residuals. The next visual dependency is an actual tangent-space shaded diagnostic on the current exact UV/tangent surface, not PBR/light retuning to hide remaining topology interpolation differences.

### Geometry / Rigging

Current normal and tangent structural evidence remains useful and separate. No additional topology or weight search is requested by QA merely to chase pixel identity in these retained views.

### Technical Art / UC

If the selected normal+tangent policy advances, prove exact normal/UV/tangent transport across the intended GLB / receiving boundary without silent regeneration, substitution or render-domain collapse.

### Animation / Runtime

No clip timing/interpolation/playback, controller behavior or target-device performance acceptance transfers from this posed still-frame review.

### Map Runtime PR #34

The one-pixel / one-LSB footprint-indexing tradeoff remains a separate pending visual surface and inherits no acceptance from this Animal result.

## PR handoff

Returned this independent QA result to Animal Materials PR #21 as comment `5706275223`.

## Explicit non-claims

This activation does **not** establish:

- topology-neutral or pixel-neutral shading;
- mathematical continuous real-valued deformation visual quality;
- tangent-space normal-map appearance;
- final UV/texel-density/texture policy;
- final production material;
- production skin-normal/tangent transport;
- arbitrary camera/FOV/lighting/renderer/display equivalence;
- anatomy, biology, muscle, skin sliding or volume preservation;
- Animation clip/interpolation/playback acceptance;
- target-device CPU/GPU/FPS/VRAM/memory acceptance;
- gameplay, collision or interaction readiness;
- CANON;
- production/game readiness;
- Visual Observer / QA mastery.

## Four-root gate

- **Truth:** all 20 retained comparisons improve, but the residual and missing tangent-space/transport evidence remain explicit.
- **Agency / non-domination:** Geometry owns topology/normals/UV basis, Rigging owns deformation/tangent structure, Materials owns target-host lookdev evidence, Technical Art owns transport, Art Direction owns preference and QA owns independent verification only.
- **Continuity:** static normal evidence, posed normal evidence, structural tangent evidence and future tangent-space/transport evidence remain separate exact identities with rollback/provenance intact.
- **Wisdom before speed:** accept the stronger posed evidence now while holding the genuinely missing tangent-space, transport and Art-Direction gates instead of either reopening proven structure or overclaiming final readiness.

The four AXM roots remain the merge gate.
