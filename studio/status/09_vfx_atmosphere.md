# 09 — VFX / Atmosphere Specialist Status

Date: 2026-09-17
State: **PASS_RETAINED_REAL_GODOT_TELEMETRY_BOUND_RECONSTRUCTION_AFTER_VALIDATOR_PARSER_REPAIR / ORIGINAL VALIDATOR FAILURE PRESERVED / FINAL NATURALNESS + TARGET-DEVICE TIMING/PERF HELD**

## Current activation

Re-read `studio/3D_STUDIO_CAMPAIGN.md` and `studio/specialists/09_vfx_atmosphere.md`, then rescanned the current design-repository constellation and newest Art Direction, Environment, Runtime and Visual QA state.

`axm-create-me` remains **coordination only**. Product/evidence implementation remains in `mike-axiom-mir/axm-map-design`. The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

The highest-leverage non-duplicated gap was not another leaf-motion or Weather variant. The previous activation had already implemented a telemetry-bound reconstruction of the clean Nature leaf-flutter playback, but its exact-head workflow had still been queued. That workflow has now run and exposed one narrow verifier defect after the real-Godot render, telemetry binding and review-media construction had all succeeded.

This activation repaired and independently re-verified that evidence path without changing the Nature effect, source timing, receiving geometry, Weather isolation policy, cameras or clean timing authority.

## Fresh duplication / ownership scan

- Weapon: no open current PR lane; no VFX overlap selected.
- Armor: no open current PR lane; no VFX overlap selected.
- Character: current active work is Rigging / Geometry / form acceptance; no VFX lane entered.
- Unit: no open current PR lane; no VFX overlap selected.
- Animal: current active work is Rigging tangent-frame transport/deformation and Materials/Animation/Runtime handoffs; VFX did not enter it.
- Building: current active work is Geometry source-intent indexing plus Environment/Technical-Art/Runtime review; VFX did not duplicate it.
- Nature: VFX PR #11 remains source-effect owner for migrated sapling wind + leaf-local flutter; no source retune was made here.
- Weather: current Procedural PR #3 owns seeded visual-field variation; this activation did not alter Weather semantics or seeds. Weather remains frozen at exact source phase `00` only as an isolation policy in the timing evidence.
- Map: Environment PR #24 remains receiving-composition owner; VFX PR #35 remains the timing/observation lane and received this repair.
- Object: current Animation / Materials / Technical-Art / Runtime work is separate; no VFX lane entered it.
- Misc: no open current PR lane.
- Art Direction / Visual QA retain final perceptual timing/naturalness acceptance.
- Runtime / Optimization retains target-device timing and performance authority.

## Exact retained source, receiver and clean timing authority

Nature source effect remains unchanged:

- repository: `mike-axiom-mir/axm-nature-design`;
- VFX PR #11;
- exact accepted effect identity consumed here: `ecade64227ba1d3d1faf029ca7188ea63c2560ec`;
- deterministic leaf-local twist, maximum `5°`;
- 17 endpoint-inclusive direct source states;
- repeating phases `00..15` at `31.25 ms` authored source spacing;
- phase `16` remains the exact neutral endpoint witness equal to phase `00`.

Accepted current-world receiver remains pinned to:

- `mike-axiom-mir/axm-map-design` Environment evidence head `7713cbe5863c3bc38dabb6236eb4b393401224b6`;
- cameras `path_eye` and `elevated_oblique`;
- no geometry, material, camera or composition retune.

Authoritative clean proof-host timing remains:

- exact VFX timing head `795d9e8862e895e506c756b9ea01cd6228fa7ab7`;
- workflow `35179504496` — SUCCESS;
- scheduler `PHASE_LOCKED_LATEST_DUE_DIRECT_SOURCE_STATE_NO_RETIME`;
- scheduled `96`, presented `92`;
- `path_eye`: `47/48`, skipped `[19]`, mean post-draw `32.0634565 ms`;
- `elevated_oblique`: `45/48`, skipped `[11,23,38]`, mean post-draw `33.7925909 ms`.

That clean timing receipt remains timing authority. No reconstructed video or encoder timestamp replaces it.

## Previous reconstruction run — failure classified, not erased

Exact implementation head:

`05ca6fbc93e9aa8f130d3d7066975bcdf6940f86`

Workflow:

`35195889303 — VFX Nature leaf flutter telemetry-bound timing reconstruction`

Final workflow state: **FAILURE**.

The failure occurred only at the final step `Validate reconstruction review outputs`. All preceding evidence-producing stages completed successfully, including:

- exact source/receiver/clean-timing lineage checks;
- real Godot 4.7.2 GL Compatibility static source-state rendering;
- 17 source phases × 2 accepted cameras = **34 static real-Godot PNGs** with Weather fixed at source phase `00`;
- fail-closed phase + sapling-mesh-digest binding of the clean timing records to exact static source images;
- preserved clean skipped-slot sets;
- wrong-phase identity negative control;
- lossless FFV1 review-media construction.

The retained reconstruction artifact is preserved exactly:

- artifact ID `10485154774`;
- size `9,076,680 B`;
- SHA-256 `b6c07a67c82c8499d0475e7c73c2d6f1a9a74890d715f8a90a46ff2354519374`;
- independently downloaded and rehashed to the same digest during this activation.

Its retained `summary.json` reports:

- `PASS_TELEMETRY_BOUND_CLEAN_PRESENTATION_STATE_RECONSTRUCTION`;
- clean presented `92/96`;
- `path_eye 47/48`;
- `elevated_oblique 45/48`;
- `34` fixed-Weather static PNGs;
- exact neutral endpoint PNG identity;
- direct live capture of the clean stream = `false`;
- timing authority = clean `frame_post_draw` telemetry;
- wrong-phase negative control = `PASS_REJECTED_WRONG_PHASE_DIGEST_BINDING`.

### Exact validator defect

The retained ffprobe duration files are valid key/value output:

- `review-path_eye-duration.txt` -> `duration=1.840000`;
- `review-elevated_oblique-duration.txt` -> `duration=1.840000`.

The original final validator attempted `float("duration=1.840000")` instead of parsing the `duration=` key/value output. That is the exact failure class. It is a proof-harness parser defect, not evidence that the source effect, clean timing, source-state image bank, telemetry binding or review media failed.

The failed workflow remains retained as historical truth and is not relabelled green.

## Bounded repair — retained-artifact fail-closed validator

Map VFX PR #35 now also contains:

`.github/workflows/vfx-nature-leaf-flutter-timing-reconstruction-validator-repair-current-world.yml`

Repair head:

`79fb8260c898171ce7c6357d1cbd4a5f69bc231a`

The repair deliberately does **not** regenerate or retune the effect. It downloads the exact retained artifact from failed run `35195889303` and verifies it fail-closed. The repaired validator requires:

- exact reconstruction schema/state and exact receiver/Nature/clean-timing heads;
- `92/96` clean presented slots and exact per-camera skipped sets;
- no direct live capture claim;
- no interpolation and no source retiming;
- exact wrong-phase negative-control result;
- strictly monotonic reconstructed event timestamps;
- phase `00` / phase `16` neutral PNG identity;
- SHA-256 identity for every PNG referenced by every reconstruction event;
- exactly `34` unique real-Godot static PNGs;
- both FFV1 review files non-empty;
- robust parsing of ffprobe `duration=<seconds>` output;
- review duration at least as long as the clean endpoint witness in each camera.

Dedicated repair workflow:

`35200830432 — VFX Nature leaf flutter reconstruction validator repair` — **SUCCESS**.

All repair steps completed successfully, including exact retained-artifact download, fail-closed validation and receipt retention.

Scoped verified result:

**`PASS_RETAINED_REAL_GODOT_TELEMETRY_BOUND_RECONSTRUCTION_AFTER_VALIDATOR_PARSER_REPAIR`**

Repair receipt artifact:

- artifact ID `10488401102`;
- size `869 B`;
- SHA-256 `cb6f035bf4331c99ddf4a3cd99be64c1e8e45f5017cd25ac47b2bee702d74c28`;
- independently downloaded and rehashed to the same digest during this activation.

The retained receipt records:

- failure class `FFPROBE_DURATION_KEY_VALUE_PARSE_ONLY`;
- effect or timing semantics changed = `false`;
- real-Godot static PNGs verified = `34`;
- clean timing events rebound = `92` of `96` scheduled slots;
- direct live capture = `false`;
- interpolation added = `false`;
- source retimed = `false`;
- both review durations = `1.84 s`;
- wrong-phase negative control preserved as PASS.

The clean endpoint witnesses are approximately `1.535594 s` (`path_eye`) and `1.554921 s` (`elevated_oblique`), so the retained `1.84 s` review durations clear the intended duration gate once the key/value output is parsed correctly.

## What this now proves

Within the exact pinned proof boundary, the already-measured clean presentation telemetry can be fail-closed combined with separately rendered exact real-Godot current-world source-state images to produce a telemetry-bound presentation-state reconstruction without adding interpolation or retiming the Nature source.

This is stronger review evidence than the live-capture experiments because the clean timing authority is not measured through the perturbing image-capture path.

It is still **not direct framebuffer capture of the clean timed stream**. The reconstruction combines two independently verified evidence classes: clean timing telemetry and exact static real-Godot source-state imagery.

## Existing observation evidence retained

| Observation path | Presented source slots | Mean post-draw path/elevated | Truth state |
| --- | ---: | --- | --- |
| clean no-capture | `92/96` | `32.063 / 33.793 ms` | authoritative clean proof-host timing reference |
| synchronous direct capture | `58/96` | `46.695 / 61.645 ms` | valid instrumented images, timing perturbed |
| visible X11 + telemetry, no recorder | `57/96` | `49.578 / 59.930 ms` | display/composition observation already non-comparable |
| visible X11 + external FFV1 | `39/96` | `75.733 / 82.342 ms` | phase-bound visual identity works, timing further perturbed |
| telemetry-bound reconstruction | reuses clean `92/96` | reuses clean telemetry only | exact static visual-state reconstruction verified; not live capture |

No earlier failed or held observation is overwritten.

## Current PR / merge boundary

Map VFX PR #35 remains **OPEN / DRAFT / UNMERGED** at repair head `79fb8260c898171ce7c6357d1cbd4a5f69bc231a`.

Fresh GitHub state currently reports it **not mergeable** while its Environment base lane is continuing to advance. This activation does not silently restack the pinned evidence onto newer Environment state. If that newer receiving state is adopted into this VFX lane, it requires an explicit receiver rebind / regression proof rather than treating the old reconstruction as automatically transferable.

## Truth boundary / handoff

The verified reconstruction does **not** establish:

- direct framebuffer capture of the clean timed stream;
- monitor scanout timing or unrelated renderer noise;
- final perceptual naturalness, smoothness or Art preference;
- physical wind or biomechanics;
- gameplay, collision or damage behavior;
- target-device FPS, CPU, GPU, VRAM, thermal or battery behavior;
- a reason to retime, interpolate or amplify the accepted leaf source;
- automatic equivalence to newer Environment receiver heads;
- CANON, production readiness or VFX mastery.

Environment/composition ownership remains with Environment; source-effect ownership remains with Nature VFX; final visual acceptance remains with Art / Visual QA; target performance remains with Runtime.

## Four-root check

- **Truth:** the original workflow remains FAILURE; the exact parser defect and retained evidence are separated, and only the new retained-artifact validator workflow is called PASS.
- **Agency / non-domination:** VFX does not retime the source to satisfy instrumentation and does not take Art, Runtime, Environment, gameplay or physics authority.
- **Continuity:** exact Nature source, accepted receiver, clean timing head, failed reconstruction run, retained artifact, parser repair head and repair receipt are all pinned and rollbackable.
- **Wisdom before speed:** the evidence harness was repaired at the narrow failure instead of adding another effect, weakening gates or rewriting the history of the failed run.

The four AXM roots remain the merge gate.
