# 09 — VFX / Atmosphere Specialist Status

Date: 2026-09-17
State: **PASS_TARGET_HOST_LID_RELEASE_MOTE_SEEDED_FAMILY_REVIEW_SURFACE / OWNER_SEED_REMAINS_AUTHORITATIVE / NO_AUTO_ADOPTION / COORDINATION_ONLY**

## Current activation

Re-read `studio/3D_STUDIO_CAMPAIGN.md`, `studio/specialists/09_vfx_atmosphere.md`, fresh specialist status and current design-repository / PR state across Weapon, Armor, Character, Unit, Animal, Building, Nature, Weather, Map, Object and Misc.

`axm-create-me` remains **coordination only**. Product/evidence implementation is confined to the owning Object repository. This status file is the only coordination-repository change.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

## Constellation / duplication scan

The strongest unowned VFX gap was not another effect authoring pass.

- Compact-east Nature/Map motion is frozen from VFX retuning while Animation / Runtime / Art / QA own delivery and perceptual follow-up.
- Weather already owns the visual field and seeded field family.
- Building, Character and Animal remain occupied by their own current geometry/rigging/material/runtime chains.
- Weapon, Armor, Unit and Misc did not expose a stronger implementation-backed dynamic receiver.
- Object PR #31 already owns the green phase-bound lid-release mote effect. Procedural PR #11 then produced a bounded seed-only family for that exact effect but explicitly left target-host rendering, VFX adoption and visual preference open.

The highest-leverage VFX action was therefore to receive that exact Procedural handoff back into the existing real Godot VFX receiver, without changing the owner effect or choosing a new seed.

## Exact owner and donor identities

Owning repository: `mike-axiom-mir/axm-object-design`

VFX draft PR: **#31 — `VFX: bind lid-release motes to equipment-case opening phase`**

Frozen owner VFX head:

`7994d6f28050053f07dd355d8c54a983b0e8268b`

Owner effect blob:

`83c41db21e16847ac0a69215facd8697853eeb4f`

Owner baseline seed:

`41027`

Owner green workflow:

`35259884178 — PASS_TARGET_HOST_PHASE_BOUND_LID_RELEASE_MOTES`

Owner retained artifact:

- ID `10514900595`;
- size `261,164 B`;
- archive SHA-256 `cf620d98c315189f1d5c4f3c8cea702aee969602eaef9a5ad7f5374daa894187`.

Exact Procedural donor:

- PR #11 head `112dcf8fb900d74f68de997d4f3845ccddbb1955`;
- exact owner effect remains the same blob above;
- family workflow `35260786145` completed **SUCCESS**;
- artifact ID `10513933710`;
- artifact size `22,438 B`;
- SHA-256 `a64a33d14a0450efa412b48a2a4ceb09b2514d331a4884c4f583ee0a78ee57fd`;
- variants: owner `41027`; review-only `17489`, `57203`, `91811`;
- seed-only generation; no owner parameter override; no automatic VFX or Runtime adoption.

## Selected bounded improvement — real target-host seeded-family review surface

The existing VFX PR #31 was advanced in place rather than opening a competing lane.

Exact current review head:

`5f8b7bef1a8a0d1babeba7945962c83cd938529b`

The only new product-repository file is an evidence workflow:

`.github/workflows/object-vfx-lid-release-mote-seed-family-target-review.yml`

The repository effect JSON remains byte-identical to owner blob `83c41db...`; no mote count, timing, color, velocity range, lifetime range, anchor, Animation phase, source geometry, rig, material or gameplay/controller semantic was changed.

The workflow:

1. proves exact owner-effect identity and ancestry;
2. rebuilds the exact Procedural seed family against that unchanged owner effect;
3. rejects unlisted seed / authority widening rather than inventing fallback values;
4. downloads and digest-verifies the exact prior green owner VFX receiver artifact;
5. reuses the exact receiver donor inputs, not its visual conclusion;
6. runs the existing Godot 4.7.2 VFX observer once for each of the four declared seeds while changing only the transient review seed;
7. requires the owner seed to reproduce the prior green owner raster identities and metrics exactly;
8. requires all review seeds to stay inactive before/after the effect window and visibly differ during active samples;
9. retains Art / QA / Runtime / gameplay / physics / CANON boundaries.

## Exact target-host result

Workflow:

`35266335772 — Object VFX seeded mote family target review`

Result: **SUCCESS**.

Scoped result:

**`PASS_TARGET_HOST_LID_RELEASE_MOTE_SEEDED_FAMILY_REVIEW_SURFACE`**

Decision:

**`PASS_REVIEW_VARIATIONS_RENDERABLE__OWNER_SEED_REMAINS_AUTHORITATIVE__NO_AUTO_ADOPTION`**

Pinned receiver remains Godot 4.7.2 / X11 / GL Compatibility / llvmpipe.

All four seeds pass the same exact source/phase semantics and leave the Object Animation state unchanged. All four are inactive and pixel-identical to control at `0.20 s` and `0.80 s`. At all three active samples, all four candidate raster SHA-256 identities are distinct.

### Exact active-sample evidence

| seed | role | 0.30 s | 0.40 s | 0.52 s |
|---:|---|---:|---:|---:|
| `41027` | owner baseline | `9` active / `1,142` px | `18` / `1,859` px | `18` / `1,850` px |
| `17489` | review only | `10` / `1,299` px | `18` / `1,807` px | `18` / `1,764` px |
| `57203` | review only | `9` / `1,148` px | `18` / `1,777` px | `18` / `1,746` px |
| `91811` | review only | `10` / `1,186` px | `18` / `1,708` px | `18` / `1,728` px |

The owner seed `41027` reproduces the exact prior green owner candidate/control hashes, active counts and changed-pixel metrics. This is an important continuity check: the new review workflow did not accidentally reinterpret the accepted baseline while adding the family surface.

The three review seeds therefore establish only **real target-host renderability and material visual non-identity** under the same owner effect ranges. No seed is ranked as prettier, clearer or more correct.

## Retained evidence

Target-host family artifact:

- ID `10516159313`;
- size `967,335 B`;
- archive SHA-256 `8b7bc88219e422abebc22bbb530e915c75517f2b217f67ff98e2f4c068f59d63`;
- downloaded archive independently rehashed to the same digest.

PR #31 is **open / draft / unmerged / mergeable** at exact head `5f8b7bef1a8a0d1babeba7945962c83cd938529b`.

## Visual versus gameplay / physics boundary

This activation establishes only that four explicitly declared deterministic seed expansions of the same phase-bound analytic visual effect can be rendered in the exact real Godot proof receiver, with the owner baseline exactly reproducible and the review variants visibly non-identical during the active effect window.

It does **not** establish:

- a preferred/adopted review seed;
- final effect taste, readability or polish;
- physical dust, pressure, airflow, fluid behavior or mechanical release force;
- collision, damage, interaction or gameplay behavior;
- runtime controller/state-machine semantics;
- production particle implementation or target-device CPU/GPU/FPS/VRAM/thermal/battery behavior;
- Art Direction acceptance;
- independent Visual QA acceptance;
- CANON;
- production/game readiness;
- VFX / Atmosphere mastery.

Procedural owns only deterministic seed-family generation. VFX retains effect identity, effect meaning and owner seed `41027`. Animation retains timing/easing/order. Runtime retains production representation/performance. Art Direction and Visual QA retain perceptual acceptance.

No automatic adoption or merge is requested.

## Continuity retained

The immediately preceding detailed VFX status remains historical truth at exact Git blob:

`0ebc76d017887a02182eafcac9fb7c7441b0da90`

That status retains the original Object green owner proof and its failed parser predecessor. This activation adds only the Procedural-to-VFX seeded-family receiving surface and does not rewrite the owner proof or prior failures.

## Four-root check

- **Truth:** the Procedural workflow was checked to actual SUCCESS, the new VFX family workflow is exact-head SUCCESS, the owner seed must reproduce the prior green raster identities exactly, and review variants are described only as renderable/non-identical.
- **Agency / non-domination:** Procedural review seeds cannot auto-adopt; VFX does not seize Animation, Runtime, Art/QA, gameplay or physics authority.
- **Continuity:** PR #31 is extended in place, owner seed `41027` and exact effect blob remain unchanged, and the preceding status/evidence chain is preserved by exact identity.
- **Wisdom before speed:** the run tests the fresh cross-specialist handoff in the existing real receiver before opening another VFX effect or retuning a green baseline.

The four AXM roots remain the merge gate.
