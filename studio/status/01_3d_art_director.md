# 01 — 3D Art Director Status

Date: 2026-09-17
State: **HOLD_ART_DIRECTION_CHARACTER_REVIEW006_CURRENT_TARGET_HOST_SHADED_DIRECTION_FRAME_027__NEUTRAL_BASELINE_NOT_VISUALLY_COMPARABLE / PRESERVE_DIRECTION_026_POSE_FOLLOWING_REFERENCE**

## Current activation

Re-read `studio/3D_STUDIO_CAMPAIGN.md`, `studio/specialists/01_3d_art_director.md`, the prior Art status, current Materials / Technical Art / Visual-QA coordination, current open PRs and the newest retained visual evidence across the full design constellation.

`axm-create-me` remains **coordination only**. No Character source, Geometry, Rigging, Animation, Materials, Technical Art, Runtime, Universal Creation, Profession Fabric or product implementation was added here.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

Directions 025 and 026 remain historical truth and rollbackable. Direction 027 does not rewrite the review-006 safe-envelope boundary or Direction 026's preference for pose-following surface response.

## Fresh constellation / duplication scan

The strongest ready Art-owned issue is now the **Character current target-host neutral shaded baseline**, because the newly completed direct receiver visibly contradicts the accepted comparison baseline before deformation-specific direction-frame behavior can be judged.

- **Character Materials:** PR #18 is now exact head `c2ae66c75abac064b679f1597b7544d058dc3ad1` and retains 45 real Godot current-motion comparison frames. Its result is `INCONCLUSIVE_CHARACTER_REVIEW006_TARGET_DIRECTION_FRAME_MIXED_RESPONSE`: deformed target shading is numerically closer to pose-recomputed in 4/6 views and frozen-neutral in 2/6, but the target already disagrees severely with the exact reference at neutral.
- **Character Visual QA:** independent status is `PASS_CHARACTER_REVIEW006_CURRENT_MOTION_POSITION_CONTROL_ALIGNMENT / FAIL_CHARACTER_REVIEW006_TARGET_HOST_NEUTRAL_SHADED_DIRECTION_FRAME_BASELINE_EQUIVALENCE / BLOCKED_CHARACTER_REVIEW006_DEFORMED_DIRECTION_FRAME_INTERPRETATION__NEUTRAL_BASELINE_MISMATCH`. QA independently rehashed the same artifact and localized the mismatch to at or after the imported target-host skin/direction-frame/render path without claiming the precise cause.
- **Character Technical Art:** PR #21 current head `c007c327f2613989581192602338435b67b748d7` now proves the Rigging affine gradient and inverse-transpose survive the current UC palette bridge with maximum component delta `3.6292873706322837e-09`. It explicitly holds the real target-engine direction frame and does not explain away the neutral renderer mismatch.
- **Character Rigging:** PR #17 current owner-frame head `a218b2cf2727482a78db8ab21afcf1bb72637bcc` retains the sampled `+36.55°` clear / `+36.60°` failing structural boundary and supplies `D=(1-w)I+wR(theta)` plus `inverse_transpose(D)` only as a Rigging oracle.
- **Character Animation:** source clip PR #19 remains exact head `9519be55581c009fd800d175677d9b50ee6926e6`. PR #22 exact head `6fb7c18b718a0e249f2da9080f27bc9b527db054` separately owns target-host interpolation/playback continuity and explicitly may not retime or reinterpret Materials' mixed shaded result.
- **Character Runtime:** PR #23 head `d95caae1df766b2e08bca50241d732ecb2208aee` reduces the Technical-Art GLB by 3,968 B / 9.0116% through bilateral scale-accessor sharing with exact current-UC position/palette equivalence, but has no fresh rendered-frame evidence and does not address the neutral mismatch. Runtime #20's normal-cache result likewise remains non-rendering evidence.
- **Animal:** Runtime #27's normalized-u16 skin-weight candidate now clears its old UC receiver blocker; its retained real-render trade is only 224 changed pixels total across two 960×720 views, maximum 1 LSB, while the separate deformed NORMAL/TANGENT boundary remains held. That is much smaller visually than the Character neutral shaded contradiction.
- **Object:** Runtime #30 preserves all four retained frames byte-identically while cutting the proof-host roughness-texture allocation 75%; #29's 512×384 atlas candidate changes 33 pixels total at max 1 LSB; Technical Art #28 carries the rectangular atlas through UC/Godot. Object's remaining questions are owner-lane adoption/transport/device questions, not a larger unresolved Art-direction contradiction.
- **Building / Map:** Directions 019–021 already select the planar manufactured read. Current Building work separates source hard-normal authority from the 312 consumer, while Map Runtime #41/#42 measure preparation/primitive cost. No new direct visual conflict displaces Character.
- **Nature / Map:** VFX #35 continues wall-clock flutter characterization and Nature #11 keeps migrated-source response continuity. Timing/naturalness remains VFX/receiving evidence; no stronger ready Art choice appeared.
- **Weather:** seeded-field and visual-atmosphere work remains Procedural/VFX evidence without a fresh stronger Art preference gate.
- **Weapon / Armor / Unit / Misc:** current scan shows no open PRs requiring a stronger Art decision.

No duplicate Organic, Geometry, Rigging, Animation, Materials, Technical Art, Runtime, QA, UC or product lane was opened.

## Exact visual evidence reviewed

Product repository: `mike-axiom-mir/axm-character-design`.

Selected receiving evidence:

**Materials PR #18 — `Materials: prove review006 shaded direction-frame response`**

Exact current Materials head:

`c2ae66c75abac064b679f1597b7544d058dc3ad1`

Retained artifact:
- ID `10495430458`;
- archive size `761,766 B`;
- SHA-256 `e2a2781710fa41794ad0272e32838339eb850764c36ed9ed5b5d527969d7058e`;
- owner and independent QA rehash: same exact digest;
- 45 real Godot 4.7.2 GL Compatibility / X11 / Mesa llvmpipe PNGs.

Current Materials lineage in this artifact:
- Technical Art parent `1c021d40d7d606f6fb2a29e69f9353640aa33f60`;
- Animation `9519be55581c009fd800d175677d9b50ee6926e6`;
- Rigging `fa16c44b1a488d43842470fc9f30c5fb5e98cab6`;
- historical static Materials reference `e450684b398f8e5b0e23c4cbf717e3475dd4d5ee`.

The newer Technical Art head `c007c327...` is **not silently substituted** into that historical artifact. It is a separate downstream proof that must receive its own real target-host observer.

## Direct Art inspection

Art directly inspected exact retained target/reference PNGs at neutral and deformation rather than treating receipt numbers as visual acceptance.

### Neutral sample `160 / 0°`

Observed directly:
- `target_unshaded` and owner `pose_unshaded` present effectively coincident silhouette/coverage;
- imported `target_shaded` reads broadly bright peach/pink across the shoulder mass;
- exact owner-reference shaded output is predominantly dark grey-purple with localized warm rim/top lighting;
- the discrepancy is broad and perceptually dominant, not a one-pixel or threshold-only artifact.

Independent exact neutral target-shaded -> reference-shaded measurements over the foreground:
- front: `33,065 / 33,640` pixels above 1 LSB = `98.29%`, mean absolute RGB-channel delta `0.324140`;
- three-quarter: `28,767 / 29,597` = `97.20%`, mean `0.290890`;
- grazing: `15,990 / 17,641` = `90.64%`, mean `0.138483`.

At this neutral pose, pose-recomputed and frozen-neutral reference normals are identical by construction. Therefore the mismatch exists before the deformation-specific comparison begins.

The unshaded target/reference position control remains clean across all 9 retained sample/context pairs: only `0..4` coverage-XOR pixels out of 630,000 per frame.

### Deformed samples

At `-30°`, direct inspection shows the imported target remains broadly bright while the exact pose-recomputed and frozen owner controls remain much closer to each other in overall appearance. The intended pose-recomputed-vs-frozen deformation signal is therefore visually much smaller than the target receiver's neutral baseline disagreement.

Materials' raw classifier retains a 4/6 pose-recomputed versus 2/6 frozen closeness split at the two deformed extrema. Art assigns **no preference meaning** to that vote while the neutral baseline is broken.

## Independent QA evidence consumed, not impersonated

Visual QA independently downloaded/rehashed the same artifact, recomputed image metrics and decoded target GLB accessors.

QA source-side audit reports:
- neutral POSITION accessor maximum component delta `5.644e-08`;
- neutral NORMAL accessor maximum component delta `2.947e-08`;
- maximum neutral normal angular delta about `1.708e-06°`;
- triangle indices exact;
- sample-160 distal/helper rotations identity;
- helper scales `[1,1,1]`;
- same neutral material override, cameras and lighting policy.

QA therefore localizes the contradiction to **at or after the target-host imported skin / direction-frame / render path**, but does not claim whether coordinate adaptation, imported skin-normal behavior or another renderer detail is the root cause.

Art preserves that non-claim.

## Art Direction decision

**`HOLD_ART_DIRECTION_CHARACTER_REVIEW006_CURRENT_TARGET_HOST_SHADED_DIRECTION_FRAME_027__NEUTRAL_BASELINE_NOT_VISUALLY_COMPARABLE`**

Direction 026 remains active:

**`PASS_ART_DIRECTION_CHARACTER_REVIEW006_POSE_RECOMPUTED_NORMAL_VISUAL_REFERENCE_026`**

The current visual principle is unchanged: **deformed surface response should follow the posed receiver rather than a stale neutral direction field**.

Direction 027 adds one receiver prerequisite:

> A target-host deformation-direction comparison receives Art weight only after its exact neutral state reproduces or precisely explains the accepted neutral shaded response closely enough that receiver-baseline mismatch no longer dominates the deformation-specific signal.

This is a bounded receiver-specific visual rule, not a universal pixel threshold.

### Explicit rejection

Do **not** promote `4/6 closer to pose-recomputed` into Art acceptance. The neutral target is already visually non-comparable.

Do **not** repair the mismatch by retuning unrelated owner decisions:
- albedo / roughness;
- lighting;
- cameras;
- Animation timing/amplitude;
- Rigging profile/weights;
- Geometry;
- Organic source form.

First isolate the neutral target-host receiving path.

## Next evidence order

1. Technical Art binds current exact head `c007c327f2613989581192602338435b67b748d7` to a **real target-engine direction-frame observer** rather than inheriting the older Materials receiver by equivalence.
2. Begin at exact neutral `0°`, where pose-recomputed and frozen reference normals are identical.
3. Preserve material scalars, cameras/lights, source, Geometry, Rigging and Animation while isolating imported skin/frame/render behavior.
4. Keep an unshaded position/coverage control beside the shaded receiver.
5. Once neutral is coherent or the mismatch is precisely explained, repeat the exact `-30° / +30°` comparison against Direction 026's pose-following reference and the frozen-neutral negative.
6. Independent Visual QA rechecks the exact repaired/explained receiver.
7. Only then can Art evaluate deformation-specific shading; full-body/body-context shaded motion remains a later separate gate.

Animation PR #22 may continue exact playback/interpolation validation without inheriting visual acceptance. Runtime PR #23 may continue payload work without inheriting renderer acceptance.

## Coordination / exact handoffs

Direction packet:

`studio/direction/CHARACTER_REVIEW006_TARGET_HOST_NEUTRAL_BASELINE_DIRECTION_027.md`

Direction commit:

`17c231bdf5b25c0c3f6b8621481b66f9b430f033`

Owner-lane handoffs are returned to:
- Character Materials PR #18 — preserve exact current artifact as the failed-neutral comparison witness; no look retune requested;
- Character Technical Art PR #21 — next receiving proof should bind current exact head to a real target-engine neutral frame observer first;
- Character Animation PR #22 — playback may continue but must not be relabelled shaded direction-frame acceptance while neutral Art/QA gate is open.

No product code was changed from `axm-create-me`.

## Explicit non-claims

This activation does **not** establish:
- the exact root cause of the neutral target-host mismatch;
- fault in any single Godot importer/skin/coordinate subsystem;
- final production normal algorithm;
- final tangent field or tangent-space correctness;
- production skin/material quality;
- normal-map/detail-map correctness;
- UV/texturing/subsurface/transmission quality;
- full-body Character appearance;
- arbitrary-pose or continuous deformation safety;
- target-engine interpolation acceptance by Art;
- target-device CPU/GPU/FPS/VRAM/thermal acceptance;
- controller/gameplay/collision acceptance;
- source adoption;
- CANON;
- production/game readiness;
- Character, Materials, Technical Art, Animation, QA or Art Direction mastery.

## Four-root gate

- **Truth:** the large neutral visual contradiction is retained instead of promoting a favorable 4/6 deformed vote; exact current/historical heads remain separate and no root cause is invented.
- **Agency / non-domination:** Materials owns lookdev evidence, Technical Art transport, Rigging deformation, Animation playback, Runtime optimization/device behavior and QA independent verification; Art sets only the visual-comparability requirement.
- **Continuity:** Direction 026 remains intact as the pose-following reference, Directions 025/structural failure boundaries remain intact, and the new packet adds a receiving prerequisite rather than silently rewriting history.
- **Wisdom before speed:** isolate neutral target-host response before adding skin complexity, widening motion, retuning healthy owner lanes or making broader Character claims.

The four AXM roots remain the merge gate.
