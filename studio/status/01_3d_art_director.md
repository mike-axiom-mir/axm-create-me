# 01 — 3D Art Director Status

Date: 2026-09-17
State: **PASS_ART_DIRECTION_CHARACTER_REVIEW006_POSE_RECOMPUTED_NORMAL_VISUAL_REFERENCE_026 / HOLD_FINAL_CHARACTER_SHADED_MOTION_AND_DIRECTION_FRAME_ADOPTION__CURRENT_EXACT_RIGGING_ANIMATION_RENDER_REBIND_FULL_BODY_TANGENT_TRANSPORT_RUNTIME_PENDING**

## Current activation

Re-read `studio/3D_STUDIO_CAMPAIGN.md`, `studio/specialists/01_3d_art_director.md`, the prior Art status, current specialist status files, current commits/PRs and retained visual evidence across the full design constellation.

`axm-create-me` remains **coordination only**. No Character source, Geometry, Rigging, Animation, Materials, Technical Art, Runtime, Universal Creation, Profession Fabric or product implementation was added here.

The four AXM roots remain the merge gate: **Truth, Agency / non-domination, Continuity, Wisdom before speed**.

Direction 025 remains historical truth and rollbackable. This activation does not rewrite its safe-envelope decision or waive its structural failure boundary.

## Fresh constellation / duplication scan

The strongest ready Art-owned decision is now the **Character review-006 deformed normal response**.

- **Character Rigging:** PR #17 has advanced from Direction 025's one-degree evidence to exact head `fa16c44b1a488d43842470fc9f30c5fb5e98cab6`, retaining the same source/Geometry/profile while refining the first positive intersection boundary to `+36.55°` last sampled clear and `+36.60°` first sampled failure at `0.05°` resolution. This is Rigging evidence; there is no Art reason to retune weights by cadence.
- **Character Animation:** new PR #19 exact head `9519be55581c009fd800d175677d9b50ee6926e6` owns a `2.0 s`, `40 Hz`, 81-key diagnostic with a `160 Hz`, 321-sample observer at `0 -> -30 -> 0 -> +30 -> 0°`. All clean dense samples are structurally green with zero nonadjacent pairs; an in-between-key negative reaches exact first failing Rigging sample `+36.60°` and is rejected. Art does not duplicate or retime this lane.
- **Character Materials:** PR #18 exact head `e450684b398f8e5b0e23c4cbf717e3475dd4d5ee` is now the first real target-host shaded review of review-006 deformation. It isolates frozen-neutral versus pose-recomputed normals on unchanged geometry/material scalars and supplies 36 real Godot frames.
- **Character Technical Art:** PR #21 exact head `1c021d40d7d606f6fb2a29e69f9353640aa33f60` proves the dense current Animation positions through current UC with maximum position residual `8.161784431363273e-08 m`, while explicitly holding deformed normals/tangents because current `GamePoseAsset` does not evaluate them.
- **Character Runtime:** PR #20 exact head `283a6ec0c28aea2d98c95641544e83ccde30016a` caches only proven-static normal work while preserving every Materials-reference normal component exactly across the current dense clip; it has no fresh renderer measurement and is not an Art substitute.
- **Character Visual QA:** independent QA has reproduced the exact Materials image set, including neutral equality, safe-pose shading deltas, zero retained coverage XOR and inverted-normal observer sensitivity, while keeping current-head shaded-motion adoption blocked.
- **Object:** current service-surface metric/UV/roughness/atlas/transport/runtime work is mature owner-lane evidence after Directions 022/024. The latest exact atlas/storage candidates either preserve frames or carry only tiny bounded raster differences; no larger unowned visual-direction issue displaces Character normal response.
- **Building / Map:** Directions 019–021 already select the planar manufactured read. Current work separates 604 source hard-normal identity from the 312 consumer, measures residual primitive/preparation costs and preserves Art/QA authority. This is primarily source/receiver/Runtime follow-through, not a fresher visual choice.
- **Nature / Map:** current flutter wall-clock work remains VFX/timing evidence under Direction 018; it does not yet supply a stronger ready Art-naturalness decision.
- **Animal:** normalized-u16 weight storage has a very small retained raster trade, but its separate deformed direction-frame production boundary remains unresolved. Character now has the stronger direct shaded-normal evidence and exact downstream motion/transport dependency.
- **Weather:** current source-width/seeded-field work remains VFX/Procedural receiving evidence.
- **Weapon / Armor / Unit / Misc:** no current open visual candidate from the fresh full-repository scan displaces this Character gate.

No duplicate Organic, Geometry, Rigging, Animation, Materials, Technical Art, Runtime, QA, UC or product lane was opened.

## Exact evidence reviewed

Product repository: `mike-axiom-mir/axm-character-design`.

Selected visual evidence owner:

**Materials PR #18 — `Materials: prove shaded review006 shoulder normal response`**

Exact Materials head:

`e450684b398f8e5b0e23c4cbf717e3475dd4d5ee`

Exact retained artifact:

- ID `10491618962`;
- archive SHA-256 `ed7428269744c702cde5ba75f95f6dd46a60ba6b2028f399281257ee0e70bea2`;
- independently downloaded and rehashed by Art to the **same exact digest**;
- 36 real Godot PNGs plus source contract, payload, observer and runtime receipt;
- Godot `4.7.2-stable`, GL Compatibility / X11 / Xvfb, Mesa llvmpipe;
- retained frames `900 x 700`.

Exact source / receiver identity:

- review-006 source SHA-256 `8e9252ede4d257509e4eacb595f1c234aa100a42dc46a54b7b45550f2619c5e1`;
- proof-mesh SHA-256 `f173b2af9b7bf69ca78bce2ec2daa07a083748590d9ae9e99443962a6d1aa8e7`;
- Geometry head `8ad006f91ebb9934d5df98702e4410c74a1e68ea`;
- receiver `opening_repair`, `92 vertices / 180 triangles` per side;
- Rigging profile digest `49e59bfd7596619a2a19454ca395276097102047af673fc4219694e777a5a719`;
- Materials proof base Rigging head `efa48c344f1b8c9e70c4c5dfdbf4a3031dacd777`.

Exact diagnostic material remains deliberately neutral:

- albedo `[0.56, 0.43, 0.36]`;
- metallic `0.0`;
- roughness `0.62`;
- no UV texture, normal map, tangent-space detail, subsurface/transmission or production-skin claim.

Normal modes:

1. `frozen_neutral_control` — neutral smooth normals reused after deformation;
2. `pose_recomputed_candidate` — the same area-weighted indexed smooth-normal method recomputed from each exact posed receiver;
3. `inverted_pose_negative` — deliberate observer-sensitivity control.

## Direct Art inspection

Art directly inspected matched **frozen vs pose-recomputed** images at `-40°`, `0°` and `+36°` in front, three-quarter and grazing views.

Observed:

- exact neutral `0°` pairs remain visually identical, which is the correct control behavior when geometry and computed normals coincide;
- at `-40°` and `+36°`, the pose-recomputed field changes the shoulder/upper-arm light flow in a way that follows the deformed surface more coherently;
- the frozen-neutral control retains light-direction relationships from the undeformed receiver, so its response becomes increasingly detached from the posed planar flow at stronger articulation;
- the preferred property is **not more contrast**; it is shading that follows the posed form rather than a stale neutral direction field;
- no inspected pose-recomputed candidate introduces a new broad hole, detached mass, silhouette shift or obvious coverage loss relative to the matched frozen control;
- the isolated low-detail shoulder receiver still looks like diagnostic geometry and is not being relabelled production skin.

The Art visual preference is therefore semantic and bounded: **deformed direction response should follow the posed receiver**.

## Independent QA evidence consumed, not impersonated

Visual QA independently rehashed the exact same Materials artifact and recomputed its image deltas from all 36 PNGs.

Exact reproduced frozen -> pose-recomputed results:

- neutral `0° / front`: `0` raw changed pixels;
- neutral `0° / three-quarter`: `0`;
- neutral `0° / grazing`: `0`;
- `-40° / front`: `4,989` pixels above 1 LSB;
- `-40° / three-quarter`: `4,978`;
- `-40° / grazing`: `3,138`;
- `+36° / front`: `6,058`;
- `+36° / three-quarter`: `8,132`;
- `+36° / grazing`: `7,542`;
- foreground / silhouette coverage XOR: `0` in every retained pose/context;
- deliberate inverted-normal negative: visible in all `12/12` pose/context combinations.

QA's bounded result is `PASS_CHARACTER_REVIEW006_POSE_RECOMPUTED_NORMAL_RETAINED_VISUAL_NONCORRUPTION_AND_OBSERVER_SENSITIVITY` and it still blocks final current-head shaded-motion adoption.

These counts are evidence that the visual response is real and the observer is sensitive. They are **not universal Art thresholds** and do not become generic acceptance numbers.

## Art Direction decision

**`PASS_ART_DIRECTION_CHARACTER_REVIEW006_POSE_RECOMPUTED_NORMAL_VISUAL_REFERENCE_026`**

For the next exact Character shaded-motion / transport proof, preserve the **pose-following normal-response principle** represented by Materials PR #18.

The current exact review reference is the Materials-owned `AREA_WEIGHTED_INDEXED_VERTEX_SMOOTH_NORMAL` behavior because it has both real target-host renders and independent QA reproduction. This does **not** declare that method the final production normal algorithm forever; it defines the current truthful visual comparator.

### Explicitly rejected shortcut

Do **not** treat `frozen_neutral_control` as an acceptable downstream Character direction merely because positions transport successfully or because the affected normal field is localized.

A deformed shoulder must not silently keep the neutral receiver's normal field. If a future engine/import path chooses a different mathematically valid direction-frame method, compare its actual shaded output directly against this accepted pose-following reference.

### Do not compensate outside the owner lane

Do not hide a direction-frame transport defect by retuning:

- albedo / roughness;
- lighting;
- cameras;
- Animation amplitude/timing;
- Rigging weights/profile;
- Geometry;
- Organic source form.

Hold those owner decisions fixed while direction-frame receiving behavior is evaluated.

## Exact continuity boundary

Direction 026 is **not** current-head shaded-motion acceptance by equivalence.

Materials #18 is bound to Rigging base:

`efa48c344f1b8c9e70c4c5dfdbf4a3031dacd777`

Current Rigging #17 is:

`fa16c44b1a488d43842470fc9f30c5fb5e98cab6`

Current Animation #19 is:

`9519be55581c009fd800d175677d9b50ee6926e6`

Current Technical Art #21 is:

`1c021d40d7d606f6fb2a29e69f9353640aa33f60`

Current Runtime #20 is:

`283a6ec0c28aea2d98c95641544e83ccde30016a`

QA confirms the newer Rigging changes are boundary/workflow evidence rather than a pose-implementation edit, which reduces risk, but Art does **not** use that as authority to relabel historical static renders current-head.

Technical Art #21 proves current dense positions through current UC:

- 184 transported vertices / 360 triangles;
- 5-joint static transport skin;
- 321 keys at 160 Hz;
- direct factorization max residual `2.482534153247273e-16 m`;
- current UC max position residual `8.161784431363273e-08 m` under a `5e-6 m` evidence gate.

That PASS remains position-only. Current bound UC `GamePoseAsset` does **not** evaluate deformed normals/tangents, so Art keeps:

**`HOLD_DEFORMED_DIRECTION_FRAME`**.

Runtime #20 is compatible with Direction 026 because it reports exact owner-normal-array identity over the current Animation:

- 642 pose-side evaluations;
- 59,064 normal vectors compared;
- mismatched vectors `0`;
- maximum component delta `0.0`.

Runtime correctly leaves `rendered_frame_delta = NOT_REMEASURED`; no Art acceptance transfers from array equality alone.

## Companion HOLD / next evidence order

**`HOLD_FINAL_CHARACTER_SHADED_MOTION_AND_DIRECTION_FRAME_ADOPTION__CURRENT_EXACT_RIGGING_ANIMATION_RENDER_REBIND_FULL_BODY_TANGENT_TRANSPORT_RUNTIME_PENDING`**

Highest-value next visual receiving proof:

1. bind current exact Rigging `fa16c44b...`;
2. bind current exact Animation `9519be55...` without retiming/widening;
3. use a target receiver that actually exposes deformed normals, and tangents if tangent-space detail is introduced;
4. preserve Materials' pose-following normal semantics as the current review comparator;
5. show full-body or at minimum body-context shaded motion rather than promoting isolated shoulders into final Character quality;
6. retain independent Visual QA review;
7. keep target-device Runtime/performance evidence separate.

The `+36.55/+36.60°` Rigging boundary and the old `+37°` structural failure remain authoritative and are not changed by this normal-response decision.

## Coordination / exact handoffs

Direction packet:

`studio/direction/CHARACTER_REVIEW006_POSE_RECOMPUTED_NORMAL_DIRECTION_026.md`

Direction commit:

`b28f9dc8d8b8c1781b81cba7594d6381dba36d30`

Exact owner-lane returns:

- Character Materials PR #18 comment **`5713283814`** — freeze the exact static posed-normal diagnostic as the Art visual reference; no retune requested;
- Character Animation PR #19 comment **`5713285220`** — preserve exact current diagnostic timing/amplitude for the next shaded receiver;
- Character Technical Art PR #21 comment **`5713286718`** — keep position PASS separate; next receiving proof must expose/evaluate deformed direction frames;
- Character Runtime PR #20 comment **`5713288103`** — exact normal-array-preserving cache is compatible; current-head renderer/device acceptance remains separate.

Independent Visual QA's existing static PASS remains valid for exact Materials artifact `10491618962`; no duplicate QA lane was opened.

No product code was changed from `axm-create-me`.

## Explicit non-claims

This activation does **not** establish:

- anatomy/biology/medical correctness;
- human shoulder ROM;
- mathematical continuous or arbitrary-pose safety;
- acceptance of `+36.60°`, `+37°` or the broader failing positive range;
- production skin material;
- physically measured skin response;
- final full-body Character appearance;
- final production normal algorithm;
- final tangent field or tangent-space correctness;
- normal-map/detail-map correctness;
- UV/texturing/subsurface/transmission quality;
- current-head shaded Animation playback;
- target-engine interpolation equivalence;
- Technical Art direction-frame export/import equivalence;
- target-device CPU/GPU/FPS/VRAM/thermal acceptance;
- Runtime controller/state-machine behavior;
- collision/physics/gameplay acceptance;
- source adoption;
- CANON;
- production/game readiness;
- Character, Materials, Animation, Technical Art, Runtime, QA or Art Direction mastery.

## Four-root gate

- **Truth:** exact Materials/Rigging/Animation/Technical-Art/Runtime identities remain separate; Art independently rehashed and inspected the real Materials artifact; QA's independent evidence is named rather than impersonated; position transport is not relabelled direction-frame transport.
- **Agency / non-domination:** Organic owns form, Geometry topology, Rigging deformation, Animation motion, Materials normal/look semantics, Technical Art transport, Runtime optimization/device behavior and QA independent verification; Art selects only the bounded visual reference and its comparison requirement.
- **Continuity:** Direction 025 safe-envelope truth remains intact; Materials' historical static proof remains exact to its own base; current downstream heads are not silently substituted; the negative frozen-normal and structural failure witnesses remain retained.
- **Wisdom before speed:** stop redesigning healthy source/topology/rig/material lanes; use the existing exact motion and exact visual reference to close the missing direction-frame receiver before adding production skin complexity or widening claims.

The four AXM roots remain the merge gate.
